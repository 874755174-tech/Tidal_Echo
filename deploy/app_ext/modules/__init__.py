#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
模块层 —— 房子里的"房间"
==========================================================================

每个房间 = 一个独立文件，自己注册自己的路由与工具，**互不 import**。

    workshop.py   工作间：他做东西的地方（第一间房）

## 为什么单独一层

`app_ext/` 根下那几个文件是**载体**（表 / 身份 / 模型网关）——
它们改一次，所有东西都受影响。房间不一样：

  · 房间是**能力**，可以一个一个加、一个一个关
  · 房间崩了不该带走房子（`__init__.register` 已经吞异常，这里再保证
    "一个房间注册失败不影响另一个房间"）
  · 每个房间自带 **独立的数据目录 / 独立的验收**，不挤在同一个文件里

## 房间要怎么长出来（照 workshop.py 抄）

  1. 新文件 `modules/<room>.py`
  2. 暴露 `install(relay, public_prefix)`：
     · 路由注册在 `/app/ext/<room>/*`（**新路径，绝不覆盖别人的**）
     · 每个端点第一行 `relay.check_auth(request)`
     · 工具用 `mcp.register_tool(..., room="<room>")` 挂到**同一扇门**上
     · `install` 必须**幂等**（跑两次结果一样）—— 验收会跑两次
  3. 数据落 `/data/<room>/`（容器里 `/data` 是持久卷）
  4. `tools/<room>_check.py` 写验收，接进 `tools/verify_all.py`

⚠️ `mcp.py` 的工具重名**默认报错**（不静默覆盖）—— 见该文件 `register_tool`。
   两个房间想用同一个工具名，说明它们其实是一个房间。
"""
