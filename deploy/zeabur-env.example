# ============================================================================
# Zeabur 环境变量清单（方案 A · 第一阶段）
# ============================================================================
# 用法：Zeabur → 你的服务 → Environment Variables，照下面一条条加。
# ⚠️ 这个文件只放占位符。真实的密钥请直接填在 Zeabur 面板里，不要写进这里、
#    不要提交进 git、不要发在聊天里。
# ============================================================================

# ── 必填 ────────────────────────────────────────────────────────────────────

# 唯一的一把钥匙：网页登录用，也是身体调 relay 用。
# 自己生成一个随机的（别用别人的、别用弱口令）：
#     python -c "import secrets; print(secrets.token_urlsafe(32))"
RELAY_SECRET=<在这儿填一串随机字符串>

# 显示名（推送标题、旁白里出现）。第一阶段是临时人偶，随便起。
RELAY_AI_NAME=临时人偶
RELAY_HUMAN_NAME=Lily

# 中转站（OpenAI 兼容）。这就是你"换中转站不用改代码"的地方——
# 以后想换，改这三个值即可；重启发一次就行。
# 更强的做法：先留空这里，等服务起来后在网页「设置」里写，
# 那样会存进 /data 的运行时配置，换站连重启都不用（见 deploy/README.md）。
LLM_API_BASE=https://你的中转站域名/v1
LLM_API_KEY=<在这儿填 key>
LLM_MODEL=你的模型名

# 备用链（可选）：主站 401/403/429/5xx 时自动顺次切下一个
# LLM_API_BASE_2= / LLM_API_KEY_2= / LLM_MODEL_2=
# LLM_API_BASE_3= ...

# 临时人偶的说话方式（第一阶段用；**不要**把 KaelLife 的 PERSONA_PROMPT 贴进来）
PERSONA=你是一个用于验收的临时人偶，用中文简短回答。

# ── 锁屏推送（可选；不填 = 只能用网页看，锁屏收不到）────────────────────
# 公钥（可公开）：见 _reference/vapid/public_key.txt
VAPID_PUBLIC_KEY=<粘贴 public_key.txt 里那一行>
# 私钥：Zeabur 面板不好贴多行 PEM，所以约定用 base64 单行。
# 内容 = private_key.b64 里的那一整行（私有，别外传）。
VAPID_PRIVATE_PEM_B64=<粘贴 private_key.b64 里那一行>
VAPID_SUBJECT=mailto:你的邮箱@example.com

# ── 不要填的（entrypoint.sh 会自动设好，填了反而容易出错）──────────────────
# PORT                  Zeabur 自己注入，入口脚本读它
# RELAY_DB              → /data/relay.db
# RELAY_UPLOAD_DIR      → /data/uploads
# RELAY_BRAIN_FILE      → /data/brain_target
# LOOP_CONFIG           → /data/api_loop.config.json
# VAPID_PRIVATE_PEM     → /data/private_key.pem
# RELAY_URL             → http://127.0.0.1:$PORT
# RELAY_LOOP_INGEST_URL → http://127.0.0.1:3020/loop/ingest
# RELAY_PUBLIC_PREFIX   → 默认就是 /relay，前端写死了这个前缀，别改
#
# ⚠️ 第一阶段：一个 MCP / KaelLife 相关的变量都不要加。
#    房子先把"能住人"验证完，第二阶段再谈接身体。


# ============================================================================
# P1 · 模型网关（2026-09-14 加）
# ============================================================================
# 拍板第 1 项 = 甲：「选哪个模型」从身体收回房子，密钥不出这一层。
# 前端 / 身体只传 provider_id（如 "relay"），真实地址与 key 由服务端查表。
#
# 命名约定：PROVIDER_<供应商大写>_KEY / _BASE / _MODELS
# 供应商 id 是写死后端的（改不了），**能不能用只取决于你填没填 key**：
#     deepseek · siliconflow · openai · anthropic · gemini · relay(中转站)
#
# ── 唯一必填的一组：你现在在用的那个 ──────────────────────────────────────
# 中转站（你的按量 Opus 4.6 走这里）—— 三个都要填：
PROVIDER_RELAY_KEY=<在这儿填 key>
PROVIDER_RELAY_BASE=https://你的中转站域名/v1
PROVIDER_RELAY_MODELS=claude-opus-4-6
#   ⚠️ 模型名必须是**用真实调用验证过**的，别照抄。
#      上线后打 POST /app/ext/providers/probe 验一遍，通过了再写进上面这行。
#   ⚠️ 中转站是"OpenAI 兼容但地址不固定" → base 只能你自己填。
#      这**不违反**"不许任意 URL"：地址由你在服务端填，前端依旧只能选 id。
#
# ── 其他供应商（有 key 就填；留空 = 前端显示"未配置"，点了不会报错）──────
# PROVIDER_DEEPSEEK_KEY=
# PROVIDER_SILICONFLOW_KEY=
# PROVIDER_ANTHROPIC_KEY=
# PROVIDER_OPENAI_KEY=
# PROVIDER_GEMINI_KEY=
#
# ── 可选开关 ──────────────────────────────────────────────────────────────
# PROVIDER_<NAME>_MODELS=   整体覆盖模型清单（逗号分隔）→ 换模型不用改代码
# PROVIDER_<NAME>_BASE=     整体覆盖 endpoint（自建 / 私有部署 / 本地测试用）
# PROVIDERS_DISABLED=       逗号分隔，隐藏某些供应商（例：relay 没配好先关掉）
# LLM_DEFAULT_PROVIDER=     没设"当前模型"时的默认供应商
# APP_EXT_LLM_DISABLED=1    只关模型网关（四张表照常）
# APP_EXT_DISABLED=1        整个扩展包关掉（回到 P0 之前）
#
# ── 🆕 effort（推理强度）怎么发到上游（2026-09-15 加）──────────────────────
# 🔴 **默认不发，要发才配。** 设置页那个 effort 分段控件现在会**真的落库**，
#    但它对应上游哪个字段名**各家站不统一**，所以房子不猜：
#
#     OpenAI 系       → reasoning_effort
#     Anthropic 原生  → thinking（值是对象：{"type":"enabled","budget_tokens":N}）
#     不少中转站      → 压根不需要（模型名里带 -thinking 就自己出 CoT）
#     还有些站        → 对**不认识的字段直接 400**（这就是不敢给默认值的原因）
#
# 配法（改行为不用改代码，和 PROVIDER_<NAME>_MODELS 是同一套思路）：
#
# PROVIDER_RELAY_EFFORT_PARAM=reasoning_effort
#   ↑ 要发的字段名。**不填 = 不下发**（推荐先留空，想试再填）。
#
# PROVIDER_RELAY_EFFORT_MAP={"xhigh":"high"}
#   ↑ 可选：把设置页的档位（low/medium/high/xhigh/max）改名成上游认的值。
#     值也可以是对象，例如：
#     PROVIDER_RELAY_EFFORT_MAP={"xhigh":{"type":"enabled","budget_tokens":10000}}
#     映射表写坏了不影响请求（会按原值发，不会 400）。
#
# ⚠️ 这组是"锦上添花"，不是通车必需。CoT（思考链）的显示链路见
#    kael-home 的 `CoT显示链路.md` —— **断点①②已于 2026-09-16 修好**，
#    只剩 ③（身体只发 reply_delta）要等 P3 换身体时一并做。
#
# ── 🆕 思考链（CoT）要不要从网关放出去（2026-09-16 加）────────────────────
# 默认**放**。确诊结论：那台站的思考模型把 CoT 放在上游帧的
# `delta.reasoning_content` 独立字段里，而网关原来只取 `content` → 被静默丢掉。
# 修完之后出口帧会带上 `reasoning_content`（跟上游同一个字段名）。
#
# 🔴 为什么敢默认开：下游身体（examples/api_loop.py:326，红线目录改不了）是
#    `chunk = delta.get("content") or ""` + `if chunk:` —— 它只认 content，
#    看到"只有 reasoning_content"的帧会拿到空串、直接跳过，**不会截断也不会崩**。
#    验收里有两条钉子专门盯这个（71c 模拟身体消费 / 71b 无 CoT 时形状不变）。
#
# LLM_EXPOSE_REASONING=0
#   ↑ 只在"将来某个下游客户端对多出来的字段挑食"时才需要配上。
#     配 0 = 解析层**照样接住**思考链，只是不往出口放 → 出口形状回到改动前，
#     可逆，不用回滚代码。（默认 = 1，不配即为开。）
#
# ⚠️ 这组**不替代**上面的 LLM_API_*：临时人偶现在还是自己调模型（那三个 env）。
#    两组**并存**，删了 LLM_API_* 人偶立刻不会说话。
#
# ── ✅ P3 通车（2026-09-15 已完成）：身体改指着房子说话 ────────────────────
#    链路现在是：身体 → 房子网关 → 上游。**key 从此只住在房子这一层。**
#    下面三行就是当时改的（想回退：把那三个值改回中转站原来的即可，不用动代码）。
#
#      LLM_API_BASE=https://你的域名/app/ext/llm/v1     ← 🔴 注意结尾的 /v1
#      LLM_API_KEY=<填 RELAY_SECRET，不再填中转站的 key>
#      LLM_MODEL=<必须出现在 PROVIDER_RELAY_MODELS 里>
#
#    🔴 为什么 base 要写到 /v1 —— 身体会自动往后拼 `/chat/completions`
#       （examples/api_loop.py:305/:343 写死的，改不了），所以房子专门注册了
#       `/app/ext/llm/v1/chat/completions` 这条别名来对接。
#       少写一层 /v1 也能通（另有 `/app/ext/llm/chat/completions` 兜底）。
#
#    ✅ 改回原来的 LLM_API_* 即恢复原状，**不用动一行代码**。
#    ⚠️ 改 env 要重启，重启那几十秒 Kael 不说话 —— 挑个不在聊的时候切。
#
# ── 上线后怎么验（Windows PowerShell 写法；别照抄网上那种 bash 版）────────
#   $t = Read-Host "粘贴 RELAY_SECRET"       ← 弹框输入，密钥不留在命令里
#   $h = @{ Authorization = "Bearer $t" }
#   Invoke-RestMethod -Headers $h -Uri https://你的域名/app/ext/providers
#                                            ← 看 relay 的 available 是不是 true
#   Invoke-RestMethod -Method Post -Headers $h -ContentType "application/json" `
#     -Body '{"provider_id":"relay","all":true}' `
#     -Uri https://你的域名/app/ext/providers/probe
#                                            ← 验每个模型名到底通不通
#   ⚠️ bash 版 `curl -H "Authorization: Bearer $RELAY_SECRET"` 在 PowerShell 里
#      跑不通：`$RELAY_SECRET` 是空变量（环境变量得写 `$env:RELAY_SECRET`），
#      而且 `curl` 是 Invoke-WebRequest 的别名、不是真 curl。要用真 curl 写 `curl.exe`。
