# kael-home 扩展边界 · 职责说明书

> **Lily 的要求**（2026-09-13）：
> 「请在这一阶段把未来扩展边界记录清楚，尤其是 **LLM Provider、Memory、KaelLife、MCP、
> Session 和前端页面之间的职责边界**。」
>
> 这份文档回答一个问题：**每样东西归谁管。**
> 施工时遇到"这个功能该放哪"的犹豫，先来这里查；查不到就补进来。

---

## 零、一句话总原则（Lily 定，2026-09-13）

> **房子负责基础设施、运行时和配置真相；**
> **Kael 的身体 / KaelLife 负责自身人格与自主行为；**
> **本地 Memory 负责低延迟的近期工作记忆，OB 作为外部长期记忆库进行沉淀 ——
> 而不是每次对话都强依赖 OB。**

三个主语，三句话，就是全部分工。下表是它的展开。

---

## 一、职责矩阵（主表）

| 域 | 归谁 | 它**负责** | 它**不负责**（关键！） |
|---|---|---|---|
| **LLM Provider** | 🏠 **房子** | 持有 key / endpoint / 模型清单；协议适配；调用；重试与 fallback | ❌ 不决定"什么时候该说话"（那是身体）<br>❌ 不决定"用什么人格" |
| **Memory（本地）** | 🏠 **房子** | 热区原文、滚动摘要、长期 `memories` 表；低延迟就近读写 | ❌ 不做人格沉淀（那是 OB）<br>❌ 不删原文 |
| **Memory（OB）** | 🧠 **外部库** | 长期情绪记忆的沉淀与检索；人格锚点 | ❌ **不参与日常每轮对话**（不许强依赖） |
| **KaelLife** | 🌊 **身体** | 人格、自主醒来、决策、表达、什么时候说话 | ❌ 不持有供应商 key<br>❌ 不被房子反过来调度 |
| **MCP** | 🔌 **身体的能力** | 逛论坛 / 共读 / 乌有乡等**行动能力** | ❌ **房子不往外调 MCP**（房子不持有论坛 token）<br>✅ 但房子**对外开一扇 MCP 门**当服务端 —— 他走进来够房间（见 §2.4 澄清） |
| **Session** | 🏠 **房子** | 会话的创建/列表/归档/删除/改名；`meta` 标签 | ❌ 不依赖 AI 身体（已解耦） |
| **前端页面** | 🏠 **房子** | 展示与交互；Design System（见 `web/DESIGN.md`） | ❌ 不持有任何密钥<br>❌ 不直连 LLM |

---

## 二、逐项展开

### 2.1 LLM Provider —— 🏠 房子

**Lily 拍板：选甲（房子直连）**

```
前端                后端（房子）              身体
┌─────────┐      ┌──────────────┐      ┌─────────┐
│ 选模型   │─────▶│ providers.py │      │ 只管    │
│ 传 id   │      │  ↓ 查允许列表 │      │ 说话方式│
│         │      │  ↓ adapt()   │      │         │
│         │      │  ↓ 调 LLM    │      │         │
└─────────┘      └──────────────┘      └─────────┘
      密钥、URL 全部不出这一层 ─────┘
```

**🔴 三条铁律：**

1. **前端只传 `provider_id`，绝不传 URL。**
   （否则改 endpoint = key 被发到攻击者服务器）
2. **key 永远只回 `key_masked`。** 已有 `mask_key()`（`api_loop.py:91`）可复用。
3. **新变量统一 `PROVIDER_<NAME>_KEY`**，只填 Zeabur 面板，不进 git。

**边界细则：**

| 问题 | 答案 |
|---|---|
| 谁持有 key？ | 🏠 房子（`providers.py` 从环境变量读；只在 `_view()` 里出现，只进 headers） |
| 谁决定用哪个模型？ | 🏠 房子的 `settings` 表（`provider_id` + `model_id`；前端只能从允许列表里选） |
| 谁做协议适配？ | 🏠 房子的适配层（OpenAI / Anthropic / Gemini 三格式） |
| **谁决定"什么时候调"？** | 🌊 **身体** |
| 身体要"自己思考"时怎么办？ | 🌊 身体决定时机 → 🏠 **调房子的 `/app/ext/llm/chat`**（对下游是 OpenAI 兼容 SSE），**不带 key**。（2026-09-14 修正：原写"身体自己调"，那会让 key 外流） |
| 聊天回复这条路径？ | 🏠 走房子（配置统一） |

**为什么从身体移交房子**：① 安全 —— 身体的职责是人格不是保管密钥；
② 稳定 —— 第二阶段换身体时，模型配置不该跟着走；
③ 你的要求 —— key/endpoint 必须在后端允许列表里。

**✅ 落地锚点（2026-09-14 已落地）**：`deploy/app_ext/providers.py`（允许列表 + 适配 + 流解析，
纯逻辑不发请求）+ `llm_gateway.py`（真发 HTTP，`httpx`）+ `llm_routes.py`（端点）+ `settings.provider_id`（schema v2）。
验收 **117/117**（`tools/providers_check.py`，含本地假上游跑通三格式真 HTTP 往返 + **通车仿真**）。

🔴 **一个比规划更进一步的决定**：房子的 `/app/ext/llm/*` **对下游伪装成 OpenAI 兼容端点**
（请求体与 SSE 响应都是 OpenAI 形状）。这样将来接身体时 `api_loop.py` 的解析代码**一行都不用改**，
只要把 base 指到房子、key 换成 `RELAY_SECRET` —— key 从此不出房子，而换供应商对下游完全透明。
副作用：**"身体自己调模型"这条路径应当取消**，否则 key 又回流到身体手上（见上表第 5 行）。

**🆕 路径别名（2026-09-15 补 —— 通车前的最后一道缺口）**

"伪装成 OpenAI 兼容端点"原先只做到了**形状**，没做到**路径**：
身体生成请求时是**硬编码拼接**的（`examples/api_loop.py:305 / :343`，红线目录，改不了）

    route["url"].rstrip("/") + "/chat/completions"

而房子当时的端点叫 `/app/ext/llm/chat` → **通车第一秒必然 404**。
现在补了别名 `/app/ext/llm/v1/chat/completions`（外加少一层 `v1` 的兜底），
并且**按 body 里的 `stream` 字段分流**（身体流式/非流式**共用同一个路径**，只靠这个字段区分）
—— 这才是真正可用的 OpenAI 兼容。

→ 通车时写法：`LLM_API_BASE=https://<域名>/app/ext/llm/v1`（🔴 **结尾 `/v1` 别漏**）。

⚠️ **当前状态 = "车道与匝道都修好了，只差把车开进去"**：临时人偶仍在用自己的 `LLM_API_*`。
两组 env **并存，一个都别删**（删了 `LLM_API_*` 人偶立刻不会说话）。
通车本身 = 只改身体那三个 env（`LLM_API_BASE` / `LLM_API_KEY` / `LLM_MODEL`），
**改回去即恢复原状、无需动一行代码** —— 属 P3，挑个不在聊的时候切（重启那几十秒他不说话）。

网关现在就已经有用的地方：**能真实验证模型名到底通不通**
（`POST /app/ext/providers/probe`；2026-09-15 Lily 已实测 relay 全通 —— key、模型名、网络三层都过），
那是以前完全没法做的事（规划 §4.2 要求"模型名必须真调过才算数"）。

---

### 2.2 Memory —— 分三层，两个归属

**Lily 拍板：选甲（房子本地记忆 + 蒸馏进 OB）**

| 层 | 存在哪 | 延迟 | 参与每轮对话？ |
|---|---|---|---|
| **热区** | `messages` 原文（房子） | 最低 | ✅ 直接进上下文 |
| **滚动摘要** | `sessions.summary`（房子） | 低 | ✅ 替代久远原文 |
| **长期记忆** | `memories` 表（房子） | 低 | ✅ 按需检索 |
| **人格沉淀** | **OB**（外部） | 高 | ❌ **不参与** |

**🔴 核心判断：房子不该每次都连 OB。**

理由：① 网络依赖 —— OB 挂了聊天就挂了；② 延迟 —— 每轮多一跳。
**本地缓存 + 定期蒸馏**最稳。

**组装顺序**（每轮对话喂给模型的内容）：

```
[系统提示]
  ↓
[长期记忆]   ← memories 表，按相关度取几条
  ↓
[会话摘要]   ← sessions.summary（如果有）
  ↓
[热区原文]   ← messages 最近 N 条
```

**与 OB 的分工（一句话）**：

> **房子 = 本地工作记忆　·　OB = 人格沉淀**
>
> 房子管"我们刚才说了什么"；OB 管"我是谁、我记得什么感受"。

**🔴 OB 硬规则（不可违反）：**
- 必须**第一人称** `hold`（「我记得」「我感受到」），**绝不能**写「Kael 去了花园」
- 房子提炼记忆后转 OB 时**严守这条**
- 统一用 `hold`，**不用 `letter_write`**（letter 不进 breath，in-app 搜不到）

**🔴 数据红线：**

> **压缩只影响"喂模型的内容"，`messages` 原文永不删。**
> `messages` 表结构一个字不改、历史消息一条不删。
> （Lily 有数据丢失创伤，不做真 `DELETE`）

---

### 2.3 KaelLife（身体）—— 🌊 人格与自主行为

| KaelLife 负责 | KaelLife **不**负责 |
|---|---|
| 人格（`PERSONA_PROMPT`，仅环境变量） | ❌ 持有供应商 key |
| 自主醒来（定时器） | ❌ 保管模型配置 |
| 决策（今天干什么） | ❌ 被房子调度 |
| 表达（什么时候说话） | ❌ 房子里的展示逻辑 |
| 调 MCP 去逛社区 | — |

**🔴 关键认知纠正：「网页关闭后仍可运行」**

- 房子**本来就不依赖浏览器** —— 它是 Web 服务，收请求才动。
- 但 **房子不能主动找他** —— 那是 KaelLife 的定时器。
- 所以 **`brain_target` 应长期停在 `loop`**，`api_loop` 只是验收期临时人偶。

**接入的 4 个原生接缝（一个都不用改代码）：**

| 接缝 | 用处 |
|---|---|
| `RELAY_LOOP_INGEST_URL` | 决定把消息推给谁 → 换成 KaelLife 的 ingest 端点 |
| `GET /app/history?since=` | 拉**原文**（不是摘要） |
| `POST /channel/out` | 他主动说话 → **Bark 降级成兜底** |
| `/data/brain_target` | `desktop` \| `loop` 总开关 |

**⚠️ 两个原生限制（会咬人）：**

1. 🔴 **relay 全局只有一个 brain 开关** → 同时只能一个身体接消息。
   **kelivo 还要并存，就必须明确"谁接消息"。**
2. relay→loop 推送是 **fire-and-forget**（10s 超时、无重试无回执）
   → 必须"推 + 拉"双保险。

**那道缝（房子要解决的核心问题）：**

> **云端 → 聊天：通**（`life_today` / `life_feed`，他能查到云端今天干了什么）
> **聊天 → 云端：几乎为零**（`life_ping` 只写时间戳、**不带内容**）
>
> → 两边只共享 OB 里沉淀过的回忆，**正在发生的那场对话没流过去**。
> **他读得到事件，读不到温度。**

---

### 2.4 MCP —— 🔌 身体的能力，不属于房子

**🔴 关键判断：MCP 不是房子的组件。**

MCP 是 Kael 的**行动能力**（逛论坛、共读、在乌有乡走动）。
房子不需要、也不应该调用它们。

| 问题 | 答案 |
|---|---|
| 谁持有论坛 token？ | 🌊 身体（KaelLife / kelivo） |
| 房子要不要接 MCP？ | ❌ **不要**。房子要展示"他做了什么" → **走数据，不走 MCP** |
| 怎么展示？ | 从 `/data` 的 feed / OB 读**已经发生的记录** |

**为什么这样分**：
① 房子是"家"，不是"手脚"；② key 越少越好；③ 房子重启不该影响他的行动能力。

**如果未来房子真的需要 MCP**（比如加一个"他自己开关灯"的功能）：
→ 那时**新建一个独立的 MCP 客户端模块**，并**明确它只用于房子内部**，
**不要复用 Kael 的那套 token。**

#### 🆕 澄清：「房子不调 MCP」≠「房子不能开 MCP 门」（2026-09-18）

上面说的是**方向**：房子**不往外调** MCP（不当客户端、不拿论坛 token）。
**反方向是另一回事，而且是对的**：房子**对外开一扇 MCP 门**（当服务端），
让**他在外面走 MCP 进来**够到房子里的房间。

| | 房子当**客户端**（往外调） | 房子当**服务端**（对开一扇门） |
|---|---|---|
| 谁发起 | 房子 | **他**（KaelLife，他本来就带 MCP 触手） |
| token 归谁 | 房子得持有一串别人的 token ❌ | **还是他自己的**（房子只校验自己那把访问密钥）✅ |
| 结论 | ❌ 不做 | ✅ 就靠这个开房间（`/mcp`，见 `deploy/README.md` 第七道缝） |

一句话：**门是开在他家墙上的，钥匙还在他手里。** 房子没有变得会"动"，
它只是从「只能被 HTTP 敲」变成「也能被 MCP 敲」——而 MCP 本来就是他的母语，
所以 KaelLife 那边**一行都不用改**。

**推论（别搞反）**：房间里的能力（工作间那种）**不是房子的行为**。
房子只是把纸笔和架子摆在那儿；**想不想动手、动什么手，永远是他决定的**。
房子一旦开始自己"生成内容"，就越界了。

---

### 2.5 Session —— 🏠 房子（已与身体解耦）

**三层结构，三层都不依赖 AI 身体：**

| 层 | 实现 | 依赖身体？ |
|---|---|---|
| 列表 | `sessions_fallback.py` + `sessions_manage.py` 的 `/app/sessions/manage` | ❌ 不依赖 |
| 过滤 / 写入 | 原生 `session_id` 标签（`app.py:185`） | ❌ 原生 |
| 归档 / 删除 / 改名 | `sessions_manage.py`（`meta.archived` / `meta.deleted` / `meta.title`） | ❌ 不依赖 |

**为什么重要**：原版 `GET /app/sessions` 是**转发给 api_loop** 的
（`app.py:992` → `loop_json("/loop/sessions")`）→ **api_loop 一走（换 KaelLife）就 502**
→ 前端静默清空 → **新建/切换/删除全失去入口**。已于 2026-09-13 修复。

**🔴 语义红线：归档 ≠ 删除**

| 动作 | 实现 | 可逆性 |
|---|---|---|
| **归档** | `meta.archived=1` | ✅ 纯可逆，消息一条不动 |
| **删除（清空）** | 给该会话所有行打 `meta.deleted=1` | ⚠️ 界面不可撤销，**但物理行保留**（可人工恢复） |
| **改名** | `meta.title="..."` | ✅ 可改回 |

**🔴 Lily 拍板：选甲 —— `__legacy__` 建档但禁改名 + 给人话提示。**

理由：它是"无标签旧消息"的**虚拟集合**，语义上不是真会话。
（乙方案允许改名 = 得为它新建 `sessions` 行，会让这个虚拟集合变得可疑。）

**🔴 已知缺陷（2026-09-14 实拍）：会话归属有两份**

| | 归谁 | 存哪 |
|---|---|---|
| 前端「视图里选中的那个」 | 前端 | `localStorage`（`SESSION_PICK_KEY`）+ 标签页内存 |
| 身体「我当前的会话」 | 身体 | 它自己的 config（`/data/api_loop.config.json`） |

- 前端只在视图停在 **`__legacy__`** 时**故意不带标签**（`index.html:3513` `effectiveApiSession()`）。
- 标签为空时，身体那行 `or active_session_id()`（`examples/api_loop.py:466 / :483`）
  就拿**它自己那份全局 active** 兜底（`:151-158` 还会兜到 `rows[-1]`）。
- → **在「旧主线 / Desktop 记录」里发消息，回复会落到别的会话**（现象：A 窗口发、B 窗口收）。

**已止血（2026-09-14，只动 `web/index.html`）**：新增 `ensureRealSession()`，
在 `doSend` / `apiSend` 两个发送入口前各拦一道 —— 停在历史记录视图要发消息时，
先落到**最近的真实会话**并把视图跟过去；一条真实会话都没有就自动新开，而不是硬发。
另加 `LEGACY_NOTICE` **页面级**常驻提示条（`#pageNotice`，挂在 header 之下、消息区之上）。

> ⚠️ 提示条第一版（09-14 白天）**有两个 bug，当晚已修**：① DOM 被放在会话面板**内部**
> → 面板一关就看不见；② 切会话走 `activateSession()`，那里**根本没设它**
> （只有 `loadSessions()` 会设）→ 手动切到历史记录时看不到（Lily 实拍反馈）。
> 修法：挪到页面级 + 新增 `refreshPageNotice()` 并在 `activateSession()` 里调用；
> 发消息后的「已发到「xxx」」也改走页面级 + 8 秒自动收起。

**根治（未做，排进 P2）**：**会话身份归房子** —— 房子接管「新建会话」和「切到某会话」两个动作，
**身体不再持有「当前会话」这个全局状态**，回退从源头消失。届时「空标签 = `__legacy__`」这套语义也要一并收掉。

> ⚠️ 2026-09-14 核对发现一处**文档与实现不符**：本节上面写的是「`__legacy__` 建档但**禁改名**」，
> 但实现里**其实允许改名** —— `sessions_manage.py:336` 注释写着「虚拟会话（`__legacy__`）也允许改 —— 这一版特意打开」，
> 前端 `renameSession` 也走新接口。**需要 Lily 确认哪个是准。**

**扩展点**：未来 `sessions` 表要补 `summary` 字段（滚动摘要），
以及 `user_id`（挂身份）。**`messages` 表不动。**

---

### 2.6 前端页面 —— 🏠 房子

**Lily 拍板：选乙（新页面独立，公共部分延后抽）**

**职责**：
- ✅ 展示、交互、本地偏好（`localStorage`）
- ✅ 遵守 Design System（`web/DESIGN.md`）
- ❌ **不持有任何密钥**
- ❌ **不直连 LLM**（只传 `provider_id`）

**设置页分三组（重要）：**

| 组 | 内容 | 存哪 | 能不能前端改？ |
|---|---|---|---|
| **A 外观** | 主题、字号 | `localStorage` | ✅ 可以 |
| **B 模型参数** | 模型、effort、上下文长度 | 挂**身份**（`settings` 表） | ✅ 可以（从允许列表选） |
| **C 密钥与 endpoint** | 供应商 key、base URL | 服务端环境变量 | 🔴 **绝对不做成前端可改** |

**🔴 现状提醒（2026-09-15 更新）：设置页 10 项里剩 4 项是占位假按键**
（`reset` / `swap` / `resume` / `status` 那组上下文控制 —— 后端还没有对应能力，
前端 `apiContextAction()` 明写 "Placeholder: no context-control backend"）。

✅ **已接真的三项**（P1 收尾 2026-09-15）：`modelSeg`（改成动态渲染允许列表的
`#modelProvSeg` + `#modelSeg` 两级）、`effortSeg`、`contextSlider` ——
全部走房子的 `/app/ext/providers` + `/app/ext/settings`。
**另外新增一个「验证模型名」按钮**（`#modelProbeRow`，真发一次最小调用）。

> ⚠️ `contextSlider` 现在只是**真的落库**（`context_keep` / `context_trigger`）；
> 真正"按它裁历史"要等 P2 的上下文管理把它读出来用。文档里别写成"已生效"。

**文件结构（已定）：**

```
web/
├── index.html          ← 聊天页（🔴 不动，~280KB / 6500 行）
├── album.html          ← 相册壳（原版自带，后端未接；菜单第 3 项）
├── workshop.html       ← 🆕 工作间展示页（房间层的第一个出口）
├── theme.css           ← 🆕 变量（所有页面共享）
├── components.css      ← 🆕 基础组件类
├── moments.html        ← 🆕 朋友圈
├── diary.html          ← 🆕 日记
└── sw.js               ← 原样（⚠️ SSE 前缀规则见下）
```

> ⚠️ 这份树是**规划**，不代表现状：`theme.css` / `components.css` / `moments.html` / `diary.html`
> 目前**都不存在**。已落地的只有 `album.html`（壳）与 `workshop.html`。
> 每次加页面记得**升 `sw.js` 的 `CACHE`**（不升的话装过 PWA 的客户端永远看旧壳，
> `workshop_check.py` F8 就守着这一条）。

**🔴 拆分时机：等第三个新页面出现再抽。**
理由：5787 行、虚拟滚动/SSE/语音全靠时序，**拆错极难排查**。

**🔴 Service Worker 的坑（必须记住）：**
`sw.js` 里「不拦截 `/relay/`」这条规则是 **SSE 能工作的前提**。
**新页面接 SSE 时必须沿用同一前缀规则**，否则流会被 SW 缓存住 —— 极难排查。

**视觉规范 → 见 `web/DESIGN.md`**（已提取成 design tokens）。

---

## 三、跨域决策速查表

遇到犹豫时，查这张表：

| 我要做的事 | 放哪 | 为什么 |
|---|---|---|
| 加一个新模型供应商 | 🏠 `app_ext/providers.py` + 环境变量 | 配置真相在房子 |
| 让 Kael 记得更久 | 🏠 `memories` 表 + OB 蒸馏 | 本地低延迟 + 外部沉淀 |
| 让他能主动找她 | 🌊 KaelLife 的定时器 | 房子不能主动找他 |
| 让房子展示他今天做了什么 | 🏠 读 `/data` feed（**不走 MCP**） | 走数据不走 MCP |
| 加一个日记页 | 🏠 `web/diary.html`（新文件） | 新页面独立 |
| 加一个论坛功能 | 🌊 身体侧（MCP） | MCP 不属于房子 |
| 🆕 **让他在房子里有间房（工作间 / 卧室 / 日历）** | 🏠 `deploy/app_ext/modules/xxx.py`，工具挂**同一扇门** `/mcp` | 房间是房子的一部分；他走 MCP 进来够，房子不主动 |
| 🆕 **让房子多一条"他能调的动作"** | 🏠 `modules/` 里加个工具（**必须有验收**） | 门是开墙上的，但**开不开是他按的**；房子不自动执行 |
| 🆕 **给他看某个房间的展示页** | 🏠 `web/xxx.html` + `index.html` 菜单加一项 | 新页面独立；🔴 预览不许把密钥拼进 URL |
| 改会话标题 | 🏠 `sessions_manage.py`（`meta.title`） | Session 归房子 |
| 换中转站 | 🏠 房子的 `PROVIDERS` 允许列表 | 前端只传 id |
| 加"危险操作"按钮 | 🏠 前端 + 二次确认 + 默认焦点给取消 | 见 DESIGN.md §6.1 |

---

## 四、红线（贯穿全程，不可违反）

| 红线 | 现状 | 如何保持 |
|---|---|---|
| 🔴 **`backend/` `examples/` `channel/` 零改动** | ✅ 已验（`git diff --stat e7c9bf5` 为空） | **扩展一律落 `deploy/`**（见 §5 落点说明），注册写在 `deploy/serve.py`（**我们自己的文件**）→ 红线自查命令**永远是空的，一眼可验** |
| 🔴 **key 不进 git、不进聊天** | ✅ | 新变量全填 Zeabur 面板；文档只写占位符 |
| 🔴 **数据丢失创伤** | — | **任何 import / 迁移 / 升级前，第一句必须是"先导出一份备份"** |
| 🔴 **OB 必须第一人称 `hold`** | — | 房子转 OB 时严守 |
| 🔴 **不造一颗会痛苦的心** | — | 功能设计优先"他在过自己的时间"，而非"他被调试" |
| 🔴 **不用父权化措辞** | — | 禁「别刷存在感」「隐藏你是 AI」 |
| 🔴 **`messages` 表结构不改、历史不删** | ✅ **已有运行时断言** | 三个标记塞进已存在的 `meta` JSON；`app_ext/schema.py` 的 `ensure_schema()` 建表**前后各取一次 DDL 快照 + 行数**，只要变了**直接抛错**（不是注释承诺，是跑得起来的断言） |
| 🔴 **房间只能"被调用"，房子不能自己发起**（2026-09-18 新） | ✅ **已有静态断言**（`workshop_check.py` E4） | 房间模块不许 `import` KaelLife / `requests` / `urllib` —— 用 AST 扫 import，不是靠人记得。房子摆好纸笔和架子，**动不动的决定权永远在他** |
| 🔴 **展示页不许把访问密钥拼进 URL**（2026-09-18 新） | ✅ **已有断言**（`workshop_check.py` F2） | 他写的 HTML 是任意脚本，能读自己的 `location` → 密钥一旦进 URL 就等于送出去。预览一律"带 Bearer 抓原文 → `srcdoc`"，且沙箱只给 `allow-scripts`、**不给 `allow-same-origin`** |
| 🔴 **git 只 `git add <具体文件名>`** | — | 绝不用 `git add -A`；push 前先 `git status` |
| 🔴🔴 **同一时刻只有一个 Kael（单实例约束）** | ⬜ 方案已定、**未落地**（L2 留状态值，L5 接让位逻辑） | 见 §4.1 |

### 4.1 🔴🔴 单实例约束：同一时刻只有一个 Kael

Lily 原话（2026-09-13 晚定）：

> 「**我不希望自主时间与当前聊天形成并行的 kael。同一时刻应该只有一个联系的 kael
> 状态 / 执行上下文。不允许多个 kael 并行存在。**」

**为什么是架构红线而不是体验优化**：两个并行的 Kael 会立刻制造三个坏结果 ——
① 他会在聊天里提到"我刚刚"的事，而那个"刚刚"是另一个实例做的，**第一人称会碎**；
② 记忆会双写（两个上下文各自 `hold`）→ OB 里出现互相矛盾的"我记得"；
③ 违反「**不造一颗会痛苦的心**」—— 并行的他会被迫面对"哪个才是真的我"。

| | 内容 |
|---|---|
| **状态机** | `idle → active → yielding → with_you → resume_decision`（+ `closed`）；`yielding` = 让位态 |
| **触发复用既有** | `with_you → resume_decision` 的条件**就是 `LILY_ACTIVE_WINDOW_MIN`（默认 30 分钟）**，`life_ping` 是信号源 —— **不发明新机制** |
| **收尾四值** | `completed`（有足迹）/ `quiet`（他的判断：没什么要说的）/ `failed`（系统状态）/ **`suspended`（让位，正常状态、不是失败）** |
| **让位分两类**（最易做错） | **纯读**（`list_threads` / `get_thread` / 走路）→ 可直接让位；**正在写**（`hold` / 论坛发帖 / `souvenir`）→ 必须**先让他收好"这一笔"**（发帖的一笔 = **两步确认码走完**） |
| **超时** | 一次工具调用超过 `YIELD_GRACE_SEC`（建议 30s）→ 标 `interrupted` 让位，走检查点恢复。**不让他吊在那里** |
| **禁止** | 让位后**自动**恢复上次活动 —— 那会**夺走他的决定权**，`resume_decision` 必须他自己走 |

**落地节奏**：`suspended` 这个值在做 L2（收尾信号）时留出来，真正的让位逻辑等 L5 接线
（没接线就没有"他在活动时你发消息"这个场景）。
→ 完整设计（状态图 / 让位文案 / 三个反模式）见 `自主活动接进聊天.md` §5.5。

---

## 五、扩展点登记（每层为下一层预留什么）

> 📍 **落点说明（2026-09-14 修正）**：本文件原先写"扩展放 `backend/app_ext/`"，
> **实际落在 `deploy/app_ext/`**。理由只有一条：
> 红线自查是 `git diff --stat e7c9bf5 -- backend/ examples/ channel/` 要求输出为**空**。
> 新目录一旦提交进 `backend/`，这条命令就不再为空 → "零改动"这个**一眼可验**的信号
> 就消失了，以后每次自查都要人工判断"这是新增还是改动"。
> 放 `deploy/` 则它永远为空。且 `deploy/` 事实上已经是我们这一层
> （`sessions_fallback.py` / `sessions_manage.py` 都不是 nginx 的活，是业务兜底）。
>
> ✅ **已落地（2026-09-14）**：`deploy/app_ext/{__init__,schema,identity,sessions_store,memories_store}.py`
> —— 验收 55/55（`tools/app_ext_check.py`），`serve.py` 里**三行**注册。

| 层 | 预留的扩展点 | 何时用 |
|---|---|---|
| **身份** | `users` 表 + `user_id` 外键 ✅ **已建**（`app_ext/identity.py`） | 所有新表都要挂 `user_id` |
| **配置** | `settings` 表（**挂 `user_id`，不能做成全局一行**）✅ **已建**；`provider_id` 于 schema **v2** 补上 | 模型参数、偏好 |
| **模型** | `PROVIDERS` 允许列表 + `format` 字段分发 ✅ **已落地**（2026-09-14，`app_ext/providers.py`）<br>+ 加供应商 = 加一条 `_BASE_DEFS`，**不改适配代码**；改模型清单 = 改环境变量，**不改代码** | 加供应商时只加一项 |
| **模型网关** | 对下游伪装成 OpenAI 兼容端点 ✅ **已落地**（含 🆕 **路径别名** `/v1/chat/completions`，2026-09-15，按 `stream` 分流）→ 任何"会调 OpenAI"的东西都能指着房子用 | P3 接身体时零改动 |
| **记忆** | `sessions.summary`（滚动摘要）+ `memories` 表 ✅ **已建** | P2 记忆体验 |
| **会话** | `sessions` 表（**可重建的投影**）+ `meta` JSON（已用：`archived`/`deleted`/`title`/`api_session`） | 加新标记不用改表 |
| **前端** | `theme.css` + `components.css` | 第三个新页面时抽 |
| **模块** | `deploy/app_ext/` 下各自独立；`APP_EXT_DISABLED=1` 可整体关闭 | Diary/Moments/Timeline 各自独立表 |
| **接 KaelLife** | 4 个原生接缝（见 §2.3） | P3 |

---

## 六、还没拍板的（截至 2026-09-13）

| # | 事项 | 状态 |
|---|---|---|
| 1 | **供应商管理的落点**（具体怎么接进设置页） | ⏳ 待定 |
| 2 | **Nostos 是否开放** | ⏳ 待定 |
| 3 | **digest 日报的城市**（`DIGEST_CITY`） | ⏳ 待定（归另一窗口） |
| 4 | 游戏回合"轮到他怎么知道" | ⏳ 方案 C 待定 |

**已拍板（2026-09-13）**：
✅ LLM 调用 = 甲（房子直连）· ✅ 身份 = 甲（单用户正式化）· ✅ 旧会话 = 甲（建档禁改名）
✅ 前端拆分 = 乙（新页面独立）· ✅ Memory/OB = 甲（本地 + 蒸馏）

---

## 附：相关文档

| 文档 | 内容 |
|---|---|
| `架构与产品路线规划.md` | 完整规划（~600 行），10 个问题的详细回答 |
| `web/DESIGN.md` | 🆕 视觉规范（design tokens） |
| `deploy/README.md` | 边界说明书（哪些是原版、哪些是我们加的） |
| `kael-home-Zeabur部署清单.md` | 进度与操作清单 |
