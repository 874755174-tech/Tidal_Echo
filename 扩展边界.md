# kael-home 扩展边界 · 职责说明书

> **Lily 的要求**（2026-09-13）：
> 「请在这一阶段把未来扩展边界记录清楚，尤其是 **LLM Provider、Memory、KaelLife、MCP、
> Session 和前端页面之间的职责边界**。」
>
> 这份文档回答一个问题：**每样东西归谁管。**
> 施工时遇到"这个功能该放哪"的犹豫，先来这里查；查不到就补进来。
>
> 📌 **维护到 2026-09-21**：红线表已含 **⑧ 上下文 / ⑨ 停止重答 / ⑩ 记忆层 / ⑪ 自主活动**，
> 以及 **E2 三槽只认 Claude**、**E4/E5 世界书与 skill 插座**（见 §5 / §6）。

---

## 零、一句话总原则（Lily 定，2026-09-13）

> **房子负责基础设施、运行时和配置真相；**
> **Kael 的身体 / KaelLife 负责自身人格与自主行为；**
> **本地 Memory 负责低延迟的近期工作记忆，OB 作为外部长期记忆库进行沉淀 ——
> 而不是每次对话都强依赖 OB。**

三个主语，三句话，就是全部分工。下表是它的展开。

---

## 一、职责矩阵（主表）

| 域 | 归谁 | 它**负责** | 它**不负责**（关键！） |
|---|---|---|---|
| **LLM Provider** | 🏠 **房子** | 持有 key / endpoint / 模型清单；协议适配；调用；重试与 fallback | ❌ 不决定"什么时候该说话"（那是身体）<br>❌ 不决定"用什么人格" |
| **Memory（本地）** | 🏠 **房子** | 热区原文、滚动摘要、长期 `memories` 表；低延迟就近读写 | ❌ 不做人格沉淀（那是 OB）<br>❌ 不删原文 |
| **Memory（OB）** | 🧠 **外部库** | 长期情绪记忆的沉淀与检索；人格锚点 | ❌ **不参与日常每轮对话**（不许强依赖） |
| **KaelLife** | 🌊 **身体** | 人格、自主醒来、决策、表达、什么时候说话 | ❌ 不持有供应商 key<br>❌ 不被房子反过来调度 |
| **MCP** | 🔌 **身体的能力** | 逛论坛 / 共读 / 乌有乡等**行动能力** | ❌ **房子不往外调 MCP**（房子不持有论坛 token）<br>✅ 但房子**对外开一扇 MCP 门**当服务端 —— 他走进来够房间（见 §2.4 澄清） |
| **Session** | 🏠 **房子** | 会话的创建/列表/归档/删除/改名；`meta` 标签 | ❌ 不依赖 AI 身体（已解耦） |
| **前端页面** | 🏠 **房子** | 展示与交互；Design System（见 `web/DESIGN.md`） | ❌ 不持有任何密钥<br>❌ 不直连 LLM |

---

## 二、逐项展开

### 2.1 LLM Provider —— 🏠 房子

**Lily 拍板：选甲（房子直连）**

```
前端                后端（房子）              身体
┌─────────┐      ┌──────────────┐      ┌─────────┐
│ 选模型   │─────▶│ providers.py │      │ 只管    │
│ 传 id   │      │  ↓ 查允许列表 │      │ 说话方式│
│         │      │  ↓ adapt()   │      │         │
│         │      │  ↓ 调 LLM    │      │         │
└─────────┘      └──────────────┘      └─────────┘
      密钥、URL 全部不出这一层 ─────┘
```

**🔴 三条铁律：**

1. **前端只传 `provider_id`，绝不传 URL。**
   （否则改 endpoint = key 被发到攻击者服务器）
2. **key 永远只回 `key_masked`。** 已有 `mask_key()`（`api_loop.py:91`）可复用。
3. **新变量统一 `PROVIDER_<NAME>_KEY`**，只填 Zeabur 面板，不进 git。

**边界细则：**

| 问题 | 答案 |
|---|---|
| 谁持有 key？ | 🏠 房子（`providers.py` 从环境变量读；只在 `_view()` 里出现，只进 headers） |
| 谁决定用哪个模型？ | 🏠 房子的 `settings` 表（`provider_id` + `model_id`；前端只能从允许列表里选） |
| 谁做协议适配？ | 🏠 房子的适配层（OpenAI / Anthropic / Gemini 三格式） |
| **谁决定"什么时候调"？** | 🌊 **身体** |
| 身体要"自己思考"时怎么办？ | 🌊 身体决定时机 → 🏠 **调房子的 `/app/ext/llm/chat`**（对下游是 OpenAI 兼容 SSE），**不带 key**。（2026-09-14 修正：原写"身体自己调"，那会让 key 外流） |
| 聊天回复这条路径？ | 🏠 走房子（配置统一） |

**为什么从身体移交房子**：① 安全 —— 身体的职责是人格不是保管密钥；
② 稳定 —— 第二阶段换身体时，模型配置不该跟着走；
③ 你的要求 —— key/endpoint 必须在后端允许列表里。

**✅ 落地锚点（2026-09-14 已落地）**：`deploy/app_ext/providers.py`（允许列表 + 适配 + 流解析，
纯逻辑不发请求）+ `llm_gateway.py`（真发 HTTP，`httpx`）+ `llm_routes.py`（端点）+ `settings.provider_id`（schema v2）。
验收 **117/117**（`tools/providers_check.py`，含本地假上游跑通三格式真 HTTP 往返 + **通车仿真**）。

🔴 **一个比规划更进一步的决定**：房子的 `/app/ext/llm/*` **对下游伪装成 OpenAI 兼容端点**
（请求体与 SSE 响应都是 OpenAI 形状）。这样将来接身体时 `api_loop.py` 的解析代码**一行都不用改**，
只要把 base 指到房子、key 换成 `RELAY_SECRET` —— key 从此不出房子，而换供应商对下游完全透明。
副作用：**"身体自己调模型"这条路径应当取消**，否则 key 又回流到身体手上（见上表第 5 行）。

**🆕 路径别名（2026-09-15 补 —— 通车前的最后一道缺口）**

"伪装成 OpenAI 兼容端点"原先只做到了**形状**，没做到**路径**：
身体生成请求时是**硬编码拼接**的（`examples/api_loop.py:305 / :343`，红线目录，改不了）

    route["url"].rstrip("/") + "/chat/completions"

而房子当时的端点叫 `/app/ext/llm/chat` → **通车第一秒必然 404**。
现在补了别名 `/app/ext/llm/v1/chat/completions`（外加少一层 `v1` 的兜底），
并且**按 body 里的 `stream` 字段分流**（身体流式/非流式**共用同一个路径**，只靠这个字段区分）
—— 这才是真正可用的 OpenAI 兼容。

→ 通车时写法：`LLM_API_BASE=https://<域名>/app/ext/llm/v1`（🔴 **结尾 `/v1` 别漏**）。

⚠️ **当前状态 = "车道与匝道都修好了，只差把车开进去"**：临时人偶仍在用自己的 `LLM_API_*`。
两组 env **并存，一个都别删**（删了 `LLM_API_*` 人偶立刻不会说话）。
通车本身 = 只改身体那三个 env（`LLM_API_BASE` / `LLM_API_KEY` / `LLM_MODEL`），
**改回去即恢复原状、无需动一行代码** —— 属 P3，挑个不在聊的时候切（重启那几十秒他不说话）。

网关现在就已经有用的地方：**能真实验证模型名到底通不通**
（`POST /app/ext/providers/probe`；2026-09-15 Lily 已实测 relay 全通 —— key、模型名、网络三层都过），
那是以前完全没法做的事（规划 §4.2 要求"模型名必须真调过才算数"）。

---

### 2.2 Memory —— 分三层，两个归属

**Lily 拍板：选甲（房子本地记忆 + 蒸馏进 OB）**

| 层 | 存在哪 | 延迟 | 参与每轮对话？ |
|---|---|---|---|
| **热区** | `messages` 原文（房子） | 最低 | ✅ 直接进上下文 |
| **滚动摘要** | `sessions.summary`（房子） | 低 | ✅ 替代久远原文 |
| **长期记忆** | `memories` 表（房子） | 低 | ✅ 按需检索 |
| **人格沉淀** | **OB**（外部） | 高 | ❌ **不参与** |

**🔴 核心判断：房子不该每次都连 OB。**

理由：① 网络依赖 —— OB 挂了聊天就挂了；② 延迟 —— 每轮多一跳。
**本地缓存 + 定期蒸馏**最稳。

**组装顺序**（每轮对话喂给模型的内容）：

```
[系统提示]
  ↓
[长期记忆]   ← memories 表，按相关度取几条
  ↓
[会话摘要]   ← sessions.summary（如果有）
  ↓
[热区原文]   ← messages 最近 N 条
```

**与 OB 的分工（一句话）**：

> **房子 = 本地工作记忆　·　OB = 人格沉淀**
>
> 房子管"我们刚才说了什么"；OB 管"我是谁、我记得什么感受"。

**🔴 OB 硬规则（不可违反）：**
- 必须**第一人称** `hold`（「我记得」「我感受到」），**绝不能**写「Kael 去了花园」
- 房子提炼记忆后转 OB 时**严守这条**
- 统一用 `hold`，**不用 `letter_write`**（letter 不进 breath，in-app 搜不到）

**🔴 数据红线：**

> **压缩只影响"喂模型的内容"，`messages` 原文永不删。**
> `messages` 表结构一个字不改、历史消息一条不删。
> （Lily 有数据丢失创伤，不做真 `DELETE`）

---

### 2.3 KaelLife（身体）—— 🌊 人格与自主行为

| KaelLife 负责 | KaelLife **不**负责 |
|---|---|
| 人格（`PERSONA_PROMPT`，仅环境变量） | ❌ 持有供应商 key |
| 自主醒来（定时器） | ❌ 保管模型配置 |
| 决策（今天干什么） | ❌ 被房子调度 |
| 表达（什么时候说话） | ❌ 房子里的展示逻辑 |
| 调 MCP 去逛社区 | — |

**🔴 关键认知纠正：「网页关闭后仍可运行」**

- 房子**本来就不依赖浏览器** —— 它是 Web 服务，收请求才动。
- 但 **房子不能主动找他** —— 那是 KaelLife 的定时器。
- 所以 **`brain_target` 应长期停在 `loop`**，`api_loop` 只是验收期临时人偶。

**接入的 4 个原生接缝（一个都不用改代码）：**

| 接缝 | 用处 |
|---|---|
| `RELAY_LOOP_INGEST_URL` | 决定把消息推给谁 → 换成 KaelLife 的 ingest 端点 |
| `GET /app/history?since=` | 拉**原文**（不是摘要） |
| `POST /channel/out` | 他主动说话 → **Bark 降级成兜底** |
| `/data/brain_target` | `desktop` \| `loop` 总开关 |

**⚠️ 两个原生限制（会咬人）：**

1. 🔴 **relay 全局只有一个 brain 开关** → 同时只能一个身体接消息。
   **kelivo 还要并存，就必须明确"谁接消息"。**
2. relay→loop 推送是 **fire-and-forget**（10s 超时、无重试无回执）
   → 必须"推 + 拉"双保险。

**那道缝（房子要解决的核心问题）：**

> **云端 → 聊天：通**（`life_today` / `life_feed`，他能查到云端今天干了什么）
> **聊天 → 云端：几乎为零**（`life_ping` 只写时间戳、**不带内容**）
>
> → 两边只共享 OB 里沉淀过的回忆，**正在发生的那场对话没流过去**。
> **他读得到事件，读不到温度。**

---

### 2.4 MCP —— 🔌 身体的能力，不属于房子

**🔴 关键判断：MCP 不是房子的组件。**

MCP 是 Kael 的**行动能力**（逛论坛、共读、在乌有乡走动）。
房子不需要、也不应该调用它们。

| 问题 | 答案 |
|---|---|
| 谁持有论坛 token？ | 🌊 身体（KaelLife / kelivo） |
| 房子要不要接 MCP？ | ❌ **不要**。房子要展示"他做了什么" → **走数据，不走 MCP** |
| 怎么展示？ | 从 `/data` 的 feed / OB 读**已经发生的记录** |

**为什么这样分**：
① 房子是"家"，不是"手脚"；② key 越少越好；③ 房子重启不该影响他的行动能力。

**如果未来房子真的需要 MCP**（比如加一个"他自己开关灯"的功能）：
→ 那时**新建一个独立的 MCP 客户端模块**，并**明确它只用于房子内部**，
**不要复用 Kael 的那套 token。**

#### 🆕 澄清：「房子不调 MCP」≠「房子不能开 MCP 门」（2026-09-18）

上面说的是**方向**：房子**不往外调** MCP（不当客户端、不拿论坛 token）。
**反方向是另一回事，而且是对的**：房子**对外开一扇 MCP 门**（当服务端），
让**他在外面走 MCP 进来**够到房子里的房间。

| | 房子当**客户端**（往外调） | 房子当**服务端**（对开一扇门） |
|---|---|---|
| 谁发起 | 房子 | **他**（KaelLife，他本来就带 MCP 触手） |
| token 归谁 | 房子得持有一串别人的 token ❌ | **还是他自己的**（房子只校验自己那把访问密钥）✅ |
| 结论 | ❌ 不做 | ✅ 就靠这个开房间（`/mcp`，见 `deploy/README.md` 第七道缝） |

一句话：**门是开在他家墙上的，钥匙还在他手里。** 房子没有变得会"动"，
它只是从「只能被 HTTP 敲」变成「也能被 MCP 敲」——而 MCP 本来就是他的母语，
所以 KaelLife 那边**一行都不用改**。

**推论（别搞反）**：房间里的能力（工作间那种）**不是房子的行为**。
房子只是把纸笔和架子摆在那儿；**想不想动手、动什么手，永远是他决定的**。
房子一旦开始自己"生成内容"，就越界了。

---

### 2.5 Session —— 🏠 房子（已与身体解耦）

**三层结构，三层都不依赖 AI 身体：**

| 层 | 实现 | 依赖身体？ |
|---|---|---|
| 列表 | `sessions_fallback.py` + `sessions_manage.py` 的 `/app/sessions/manage` | ❌ 不依赖 |
| 过滤 / 写入 | 原生 `session_id` 标签（`app.py:185`） | ❌ 原生 |
| 归档 / 删除 / 改名 | `sessions_manage.py`（`meta.archived` / `meta.deleted` / `meta.title`） | ❌ 不依赖 |

**为什么重要**：原版 `GET /app/sessions` 是**转发给 api_loop** 的
（`app.py:992` → `loop_json("/loop/sessions")`）→ **api_loop 一走（换 KaelLife）就 502**
→ 前端静默清空 → **新建/切换/删除全失去入口**。已于 2026-09-13 修复。

**🔴 语义红线：归档 ≠ 删除**

| 动作 | 实现 | 可逆性 |
|---|---|---|
| **归档** | `meta.archived=1` | ✅ 纯可逆，消息一条不动 |
| **删除（清空）** | 给该会话所有行打 `meta.deleted=1` | ⚠️ 界面不可撤销，**但物理行保留**（可人工恢复） |
| **改名** | `meta.title="..."` | ✅ 可改回 |

**🔴 Lily 拍板：选甲 —— `__legacy__` 建档但禁改名 + 给人话提示。**

理由：它是"无标签旧消息"的**虚拟集合**，语义上不是真会话。
（乙方案允许改名 = 得为它新建 `sessions` 行，会让这个虚拟集合变得可疑。）

**🔴 已知缺陷（2026-09-14 实拍）：会话归属有两份**

| | 归谁 | 存哪 |
|---|---|---|
| 前端「视图里选中的那个」 | 前端 | `localStorage`（`SESSION_PICK_KEY`）+ 标签页内存 |
| 身体「我当前的会话」 | 身体 | 它自己的 config（`/data/api_loop.config.json`） |

- 前端只在视图停在 **`__legacy__`** 时**故意不带标签**（`index.html:3513` `effectiveApiSession()`）。
- 标签为空时，身体那行 `or active_session_id()`（`examples/api_loop.py:466 / :483`）
  就拿**它自己那份全局 active** 兜底（`:151-158` 还会兜到 `rows[-1]`）。
- → **在「旧主线 / Desktop 记录」里发消息，回复会落到别的会话**（现象：A 窗口发、B 窗口收）。

**已止血（2026-09-14，只动 `web/index.html`）**：新增 `ensureRealSession()`，
在 `doSend` / `apiSend` 两个发送入口前各拦一道 —— 停在历史记录视图要发消息时，
先落到**最近的真实会话**并把视图跟过去；一条真实会话都没有就自动新开，而不是硬发。
另加 `LEGACY_NOTICE` **页面级**常驻提示条（`#pageNotice`，挂在 header 之下、消息区之上）。

> ⚠️ 提示条第一版（09-14 白天）**有两个 bug，当晚已修**：① DOM 被放在会话面板**内部**
> → 面板一关就看不见；② 切会话走 `activateSession()`，那里**根本没设它**
> （只有 `loadSessions()` 会设）→ 手动切到历史记录时看不到（Lily 实拍反馈）。
> 修法：挪到页面级 + 新增 `refreshPageNotice()` 并在 `activateSession()` 里调用；
> 发消息后的「已发到「xxx」」也改走页面级 + 8 秒自动收起。

**根治（未做，排进 P2）**：**会话身份归房子** —— 房子接管「新建会话」和「切到某会话」两个动作，
**身体不再持有「当前会话」这个全局状态**，回退从源头消失。届时「空标签 = `__legacy__`」这套语义也要一并收掉。

> ⚠️ 2026-09-14 核对发现一处**文档与实现不符**：本节上面写的是「`__legacy__` 建档但**禁改名**」，
> 但实现里**其实允许改名** —— `sessions_manage.py:336` 注释写着「虚拟会话（`__legacy__`）也允许改 —— 这一版特意打开」，
> 前端 `renameSession` 也走新接口。**需要 Lily 确认哪个是准。**

**扩展点**：未来 `sessions` 表要补 `summary` 字段（滚动摘要），
以及 `user_id`（挂身份）。**`messages` 表不动。**

---

### 2.6 前端页面 —— 🏠 房子

**Lily 拍板：选乙（新页面独立，公共部分延后抽）**

**职责**：
- ✅ 展示、交互、本地偏好（`localStorage`）
- ✅ 遵守 Design System（`web/DESIGN.md`）
- ❌ **不持有任何密钥**
- ❌ **不直连 LLM**（只传 `provider_id`）

**设置页分三组（重要）：**

| 组 | 内容 | 存哪 | 能不能前端改？ |
|---|---|---|---|
| **A 外观** | 主题、字号 | `localStorage` | ✅ 可以 |
| **B 模型参数** | 模型、effort、上下文长度 | 挂**身份**（`settings` 表） | ✅ 可以（从允许列表选） |
| **C 密钥与 endpoint** | 供应商 key、base URL | 服务端环境变量 | 🔴 **绝对不做成前端可改** |

**🔴 现状提醒（2026-09-15 更新）：设置页 10 项里剩 4 项是占位假按键**
（`reset` / `swap` / `resume` / `status` 那组上下文控制 —— 后端还没有对应能力，
前端 `apiContextAction()` 明写 "Placeholder: no context-control backend"）。

✅ **已接真的三项**（P1 收尾 2026-09-15）：`modelSeg`（改成动态渲染允许列表的
`#modelProvSeg` + `#modelSeg` 两级）、`effortSeg`、`contextSlider` ——
全部走房子的 `/app/ext/providers` + `/app/ext/settings`。
**另外新增一个「验证模型名」按钮**（`#modelProbeRow`，真发一次最小调用）。

> ⚠️ `contextSlider` 现在只是**真的落库**（`context_keep` / `context_trigger`）；
> 真正"按它裁历史"要等 P2 的上下文管理把它读出来用。文档里别写成"已生效"。

**文件结构（已定）：**

```
web/
├── index.html          ← 聊天页（🔴 不动，~280KB / 6500 行）
├── album.html          ← 相册壳（原版自带，后端未接；菜单第 3 项）
├── workshop.html       ← 🆕 工作间展示页（房间层的第一个出口，菜单第 7 项）
├── archive.html        ← 🆕 导出 / 快照页（P2-0，菜单第 8 项；**不是房间**）
├── theme.css           ← 🆕 变量（所有页面共享）
├── components.css      ← 🆕 基础组件类
├── moments.html        ← 🆕 朋友圈
├── diary.html          ← 🆕 日记
└── sw.js               ← 原样（⚠️ SSE 前缀规则见下）
```

> ⚠️ 这份树是**规划**，不代表现状：`theme.css` / `components.css` / `moments.html` / `diary.html`
> 目前**都不存在**。已落地的只有 `album.html`（壳）、`workshop.html`、`archive.html`。
> 每次加页面记得**升 `sw.js` 的 `CACHE`**（不升的话装过 PWA 的客户端永远看旧壳，
> `workshop_check.py` F8 / `archive_check.py` D8 都守着这一条）。

**🔴 拆分时机：等第三个新页面出现再抽。**
理由：5787 行、虚拟滚动/SSE/语音全靠时序，**拆错极难排查**。
> ⏳ **进度**：新页面已有 **2 个**（`workshop.html` + `archive.html`，两者的主题变量是逐字复制的）
> → **下一个新页面（第 3 个）就该把 `theme.css` / `components.css` 抽出来了**，别再复制第三份。

**🔴 Service Worker 的坑（必须记住）：**
`sw.js` 里「不拦截 `/relay/`」这条规则是 **SSE 能工作的前提**。
**新页面接 SSE 时必须沿用同一前缀规则**，否则流会被 SW 缓存住 —— 极难排查。

**视觉规范 → 见 `web/DESIGN.md`**（已提取成 design tokens）。

---

## 三、跨域决策速查表

遇到犹豫时，查这张表：

| 我要做的事 | 放哪 | 为什么 |
|---|---|---|
| 加一个新模型供应商 | 🏠 `app_ext/providers.py` + 环境变量 | 配置真相在房子 |
| 让 Kael 记得更久 | 🏠 `memories` 表 + OB 蒸馏 | 本地低延迟 + 外部沉淀 |
| 让他能主动找她 | 🌊 KaelLife 的定时器 | 房子不能主动找他 |
| 让房子展示他今天做了什么 | 🏠 读 `/data` feed（**不走 MCP**） | 走数据不走 MCP |
| 加一个日记页 | 🏠 `web/diary.html`（新文件） | 新页面独立 |
| 加一个论坛功能 | 🌊 身体侧（MCP） | MCP 不属于房子 |
| 🆕 **让他在房子里有间房（工作间 / 书房 / 日历）** | 🏠 `deploy/app_ext/modules/xxx.py`，工具挂**同一扇门** `/mcp` | 房间是房子的一部分；他走 MCP 进来够，房子不主动。🔴 房间名先问过他（工作间/书房都是他自己定的） |
| 🆕 **让房子多一条"他能调的动作"** | 🏠 `modules/` 里加个工具（**必须有验收**） | 门是开墙上的，但**开不开是他按的**；房子不自动执行 |
| 🆕 **给他看某个房间的展示页** | 🏠 `web/xxx.html` + `index.html` 菜单加一项 | 新页面独立；🔴 预览不许把密钥拼进 URL |
| 🆕 **让她把自己的东西整份拿走（导出 / 快照）** | 🏠 `app_ext/archive.py` + `web/archive.html`，**只走 HTTP + 密钥** | 🔴 **给她的路 ≠ 给他的路**：房间挂 `/mcp`（他走），导出**不挂 `/mcp`**（她走）。只读、只有 GET；快照用 `Connection.backup()` 不裸拷；**导入明确不做**（覆盖 = 丢数据根因）。见 `deploy/README.md` 第八道缝 |
| 🆕 **让长对话不丢早期信息（上下文管理）** | 🏠 `app_ext/context.py` —— 在**身体必经的网关口**注入一条 system 摘要 | 🔴 热区的裁决权在**身体**（`history_n`），房子**不许裁历史**；房子只**多加**一条 system。压缩**手动**触发（房子不自己花钱调 LLM）。`sessions.summary` 落点 + `summary_upto` 记"压到哪条"。见 `deploy/README.md` 第九道缝 |
| 改会话标题 | 🏠 `sessions_manage.py`（`meta.title`） | Session 归房子 |
| 换中转站 | 🏠 房子的 `PROVIDERS` 允许列表 | 前端只传 id |
| 加"危险操作"按钮 | 🏠 前端 + 二次确认 + 默认焦点给取消 | 见 DESIGN.md §6.1 |

---

## 四、红线（贯穿全程，不可违反）

| 红线 | 现状 | 如何保持 |
|---|---|---|
| 🔴 **`backend/` `examples/` `channel/` 零改动** | ✅ 已验（`git diff --stat e7c9bf5` 为空） | **扩展一律落 `deploy/`**（见 §5 落点说明），注册写在 `deploy/serve.py`（**我们自己的文件**）→ 红线自查命令**永远是空的，一眼可验** |
| 🔴 **key 不进 git、不进聊天** | ✅ | 新变量全填 Zeabur 面板；文档只写占位符 |
| 🔴 **数据丢失创伤** | — | **任何 import / 迁移 / 升级前，第一句必须是"先导出一份备份"** |
| 🔴 **OB 必须第一人称 `hold`** | — | 房子转 OB 时严守 |
| 🔴 **不造一颗会痛苦的心** | — | 功能设计优先"他在过自己的时间"，而非"他被调试" |
| 🔴 **不用父权化措辞** | — | 禁「别刷存在感」「隐藏你是 AI」 |
| 🔴 **`messages` 表结构不改、历史不删** | ✅ **已有运行时断言** | 三个标记塞进已存在的 `meta` JSON；`app_ext/schema.py` 的 `ensure_schema()` 建表**前后各取一次 DDL 快照 + 行数**，只要变了**直接抛错**（不是注释承诺，是跑得起来的断言）。<br>🔴 **2026-09-19 收窄（⑨）**：从"一个字不碰"收窄成 **「只准写 `meta` 这一列」**（见下方⑨第一行）—— 结构、行数、正文**一丝不变**。（⑨ 那次**没有**升版本；当前是 **v6**，来自 ⑩-b 的 `distill_upto` + `superseded_by`。） |
| 🔴 **房间只能"被调用"，房子不能自己发起**（2026-09-18 新） | ✅ **已有静态断言**（`workshop_check.py` E4） | 房间模块不许 `import` KaelLife / `requests` / `urllib` —— 用 AST 扫 import，不是靠人记得。房子摆好纸笔和架子，**动不动的决定权永远在他** |
| 🔴 **展示页不许把访问密钥拼进 URL**（2026-09-18 新） | ✅ **已有断言**（`workshop_check.py` F2） | 他写的 HTML 是任意脚本，能读自己的 `location` → 密钥一旦进 URL 就等于送出去。预览一律"带 Bearer 抓原文 → `srcdoc`"，且沙箱只给 `allow-scripts`、**不给 `allow-same-origin`** |
| 🔴 **git 只 `git add <具体文件名>`** | — | 绝不用 `git add -A`；push 前先 `git status` |
| 🔴 **导出物不许靠 `?token=` 授权**（2026-09-19 新） | ✅ **已有断言**（`archive_check.py` B8 / D2） | 房子别处的 `check_auth` 允许 `?token=`（那是给 `EventSource` 的妥协）；但**下载**最顺手的写法就是 `<a href="…?token=…">` → 密钥进历史 / 书签 / Referer。所以导出端点**只认 `Authorization: Bearer`**，`?token=` 一出现直接 **400**。这是**有意与别处不同**的一条差异，不是漏配 |
| 🔴 **导出只有 GET，导入明确不做**（2026-09-19 新） | ✅ **已有断言**（`archive_check.py` C4） | 导入**覆盖**正是 08 月丢数据的根因，风险与导出完全不对称。哪天真要做，形态只有一种：**只合并不覆盖 + 先预览 + 自动备份** |
| 🔴 **快照不许裸拷 `.db`**（2026-09-19 新） | ✅ **已有断言**（`archive_check.py` A1–A7，故意用 WAL + 未 checkpoint 的写入） | WAL 下裸拷会拿到**半截**，是"备份了却打不开"的经典死法，要到真出事那天才发现。必须走 `sqlite3.Connection.backup()`（副本要能被**独立**打开、表名行数与源一致、`integrity_check` = ok） |
| 🔴 **`.jsonl` 必须一行一条（记录以换行结尾）**（2026-09-19 新） | ✅ **已有断言**（`archive_check.py` A9b / B6b） | `StreamingResponse` 只是把 yield 串起来 —— **少了换行，N 条会粘成一整行**，`grep` / `wc -l` / 逐行读全废（真踩过：18 条挤成 1 行 17KB）。**"记录本身是合法 JSON" ≠ "文件是 JSONL"** → 验收必须**数换行**（且逐行 parse、末行有换行），不能只做子串搜索 |
| 🔴🔴 **网关注入「只插不删 + fail-open」**（2026-09-19 新） | ✅ **已有断言**（`context_check.py` B2/B3/B4/B10，**从出口倒着验**） | 上下文层只往 `messages` 里**多加**一条 system，**绝不动已有消息与顺序**（动了就是"悄悄裁历史" —— 那正是"两处都以为对方在管"的经典病）。且任何异常 / 不确定（认不出会话、摘要为空、**会撑爆 `MAX_SYSTEM_CHARS`**）都必须**原样放行**：撑爆会让 `normalize_request` 抛 `too_large` → **400 → 这次说话直接失败**，"更好用"绝不能变成"说不了话" |
| 🔴 **认不出会话就跳过，不许猜**（2026-09-19 新） | ✅ **已有断言**（`context_check.py` B6 / B11） | 网关拿到的 OpenAI body 里**没有 session_id**（身体本地生成）。用"当前用户消息原文"回库反查，认的是 **`meta.api_session`**；查不到就不注入。拿"当前活跃会话"顶上会在切换会话时**张冠李戴**，注入别人会话的摘要比不注入坏得多 |
| 🔴 **上下文层不是房间**（2026-09-19 新） | ✅ **已有断言**（`context_check.py` D3 / D4） | `context.py` 不许 `import` mcp、不进 `modules/`。房间是**给 Kael 走 MCP** 的；上下文管理是**房子自己读法的一部分**。另外它源码里不许出现对 `messages` 的写语句（原文不删） |
| 🔴🔴 **摘要必须写成"他自己的记忆"**（2026-09-19 新，第二次上线补） | ✅ **已有断言**（`context_check.py` A8b/A9/A9b/A9c/A11/A12/A13） | 摘要将来是**他本人读到**的 —— 人称必须是「我」=Kael、「你」=Lily，**不许**出现「用户 / 对方 / assistant」这种档案腔，也不许写成信件/独白。第一版真压出来是「用户告诉对方（Kael）…」，等于他每次开口前先读一份关于自己的报告。🔴 **人称规约必须写在系统提示里**：只改材料标签，模型会自己漂回第三人称（实测发生过） |
| 🔴 **已有摘要只能靠 `rebuild` 重写，增量压改不掉它**（2026-09-19 新） | ✅ **已有断言**（`context_check.py` C12 / C13 / C14） | 增量压会把老摘要**一起并进**新摘要 → 写歪的腔调会一直传下去。`rebuild: true` = 忽略 `summary_upto` 与老摘要、旧消息全部重喂。敢重压的底气 = **原文从来没删**，派生数据随时可重新推导（⚠️ 它会**覆盖** `sessions.summary` 这一格 —— 那是唯一的覆盖点，且只覆盖派生数据） |
| 🔴🔴 **同一时刻只有一个 Kael（单实例约束）** | ⬜ 方案已定、**未落地**（L2 留状态值，L5 接让位逻辑） | 见 §4.1 |
| 🔴 **扩展层对 `messages` 只准写 `meta` 列**（2026-09-19 新，⑨ 起） | ✅ **已有断言**（`schema.py` 的 `with_meta_guard` + `generate_check.py` D3/D4/D5–D8） | ⑨ 必须在库里留痕（`truncated` / `superseded` / `variant`），但**原文一个字不能动**。收窄成三条硬约束：**① 行数不变 ② 正文列逐字节不变 ③ 只允许 `UPDATE messages SET meta = ?`**。守卫的写法是"动手前后各算一次**正文指纹**（`messages_body_signature`：除 `meta` 外全部列的 sha256），不一致就**回滚 + 抛错**"。（⑨ 那次**没有**升版本；当前是 **v6**，来自 ⑩-b 的 `distill_upto` + `superseded_by`。）🔴 验收里有一条**故意越界**的用例（`UPDATE messages SET text='HACKED'`）必须被拦下且已回滚 |
| 🔴🔴 **停止必须"温柔掐"，不许裸断流**（2026-09-19 新） | ⚠️ **半有断言**（`generate_check.py` B7 有牙齿；B8 对"硬断流"变异**无感**——见下） | 收尾要补一个**正常的** `finish=stop` + `data: [DONE]`，让身体以为"模型正常说完了"，那半条才能照常 POST 回来。**为什么不能裸断**：身体 `run_model` 是 `for route in main_chain()` + `except Exception` → **换链上下一个模型重跑一遍**（`examples/api_loop.py:376-381`）。<br>⚠️ **诚实记录**：我先做了变异（删掉收尾两行 → `task.cancel() + break`），结果 B7 红、**B8 没红** —— 查代码发现 `stream_chat`（`api_loop.py:312-330`）对"流没有 `[DONE]` 就结束"是**宽容的**，同机 loopback 上干净关连接 = 客户端读到一个完整的短 body，**不会抛**。真会触发 fallback 的是**协议级不完整**（声明长度却少写字节 / chunked 没终止块）→ 这才有了 B16/B17 那条对照用例。**结论：这条红线的理由成立，但"证"它的不是 B8，是 B16** |
| 🔴 **停止的定位键是 `api_session`，不是 `stream_id`**（2026-09-19 新） | ✅ **已有断言**（`generate_check.py` A11 / A3 / D15） | 身体的 `stream_id` 是它本地 `uuid4`（`examples/api_loop.py:386`），**从不往上游传**（上游 body 只有 `{model, messages, temperature, max_tokens, stream}`）→ 房子**永远不知道**那个值。所以用 ⑧ 那套 `context.session_of`（拿最后一条 user 原文回库反查 `meta.api_session`）认会话。前端传来的 `stream_id` 只当**回执**，不当定位键。认不出会话 → 退化成"停当前在飞的那一个"（单 Kael 保证唯一），**不猜** |
| 🔴 **⑨ 的范围只有"最后一条回复"**（2026-09-19 新，Lily 拍板） | ✅ **已有断言**（`generate_check.py` C7 / C13） | 对历史中间某条重答会牵连它之后**所有**轮次（那些上下文里含已作废的回复）—— 是另一个量级的事。`last_turn()` 只取"会话内最后一条 `user`/`voice` + 它之后最后一条 `reply`" |
| 🔴 **先叫身体，再标旧的那条**（顺序不可反）（2026-09-19 新） | ✅ **已有断言**（`generate_check.py` C2/C3 之前失败即一格未动） | 反过来会出现"旧的白标了、新的没来" → 屏幕上凭空少一条回复。按 `rewrite()` 的顺序，**叫不动身体时库里一格都没动**（`rewrite` 里 `_fire_ingest_sync` 抛出 → 直接 `return _fail("loop_unreachable")`，`update_message_meta` 还没执行） |
| 🔴 **启动日志必须 GBK 可编码**（2026-09-19 新，本地起得来才有得验） | ✅ **已有断言**（`generate_check.py` D13 / D14） | Windows 上子进程 stdout 是 **cp936**，而 `register()` 末尾那几行 `print` 打在 **try 之外** → 一个 GBK 里没有的字符（`⑨` / `⚠️` / `✅`）就是 `UnicodeEncodeError` → **房子起不来**（Zeabur 是 UTF-8 不会中招，本地才会）。所以 `generate.summary_line()` 与所有 `print` 一律用纯中文，符号只准出现在**注释和 docstring** 里 |
| 🔴 **在飞登记是内存态**（已知取舍，非 bug） | ⬜ 有意为之（与 `stream_drafts` 同一个 TTL 取舍） | 容器重启 → 在飞登记清空 → 重启那一刻"停不了正在生成的"。**可接受**：重启本身就把上游连接断了，"正在生成"这件事在重启后不复存在。`status` 里明说了这条，别当缺陷修 |
| 🔴🔴 **`memories` 是"房子的技术缓存"，不是"他的记忆"**（2026-09-20 新，⑩-a 起） | ✅ **已有断言**（`memory_check.py` D5 / D6） | 所以它**不挂 MCP 门**、**不给他工具**。他的记忆走 OB 的 `hold`。把这层开给他 = 让他能看见自己"**被提炼成什么样**" —— 那是被生成的，不是他自己写的，会污染「记忆主权」。**这一条是有意不做，不是漏了** |
| 🔴 **`memories` 没有 delete，而且是结构性的**（2026-09-20 新） | ✅ **已有断言**（`memory_check.py` A19 / A20 / D4；HTTP 面 C22 验 405） | `memories_store.py` 里**不存在** `delete` 函数 —— 理由是"不造一颗会痛苦的心"：一旦它存在，就会在某天被某个"清理逻辑"顺手调用。要清理走**人工**（直接操作 SQLite）。约束在**代码层面**守住，不是靠自觉 |
| 🔴 **`salience` 永不外泄**（2026-09-20 新） | ✅ **已有断言**（`memory_check.py` A29 / A30 / C7–C10，**字符串级**：响应体里不许出现这个词） | 内部权重（跟"想念度数值不做"同源）。写入**可以**传，但**任何响应都不回**。所有响应过 `memories_store.public()` 那张白名单投影 —— 新加列默认不外泄，**在安全那一侧失败** |
| 🔴 **`source` 是"缝"：形状锁死，取值不锁死**（2026-09-20 新） | ✅ **已有断言**（`memory_check.py` A7–A13 / C20 / C21） | 形状 `^[a-z0-9_-]{1,24}$`，**非法才降级**成 `chat` 且**如实报** `source_coerced` + `source_raw`；**形状合法但未知 → 原样存**（新来源接进来零改动）。🔴 **`kind` 那套"未知即归类"不能照抄**：`kind` 的枚举完备、归类是收敛的；`source` 的全部意义就是"以后能多出一种来源"，归类 = 把阅读记忆静默标成对话记忆 = **数据损坏** |
| 🔴 **加列不许带 `DEFAULT`**（2026-09-20 新，冒烟真踩） | ✅ **已有断言**（`memory_check.py` B7 / B18 / B19） | `ALTER TABLE … ADD COLUMN x TEXT DEFAULT 'chat'` 会让**已有行也报出 `'chat'`**（SQLite 对老行返回那个默认值，不重写行）→ 把「**不知道从哪来**」静默洗成「**来自对话**」。⇒ 默认值住在**代码**里（`SOURCE_DEFAULT`），`add()` 每次显式传值；**老行 = `NULL` = 不知道**，读取层**不兜底**（`stats` 归 `(unknown)`，`list(source='chat')` 不捞 NULL）。附带好处：新库/老库的列行为**完全一致**，没有"只在老库上错"的暗坑 |
| 🔴 **schema 版本号是"有意升级"的信号**（2026-09-20 新，09-25 补充） | ✅ **已有断言**（`generate_check.py` D9 / `memory_check.py` D11 **写死** `== 6`） | 那两条断言**有意写死**，守的是"**没人偷偷动表结构**"—— 谁**有意**改了就来改这两行，并在 commit message 里说明。<br>⚠️ 反过来：守"**某次迁移真发生过**"的断言要用 `>=` 或读 `S.SCHEMA_VERSION`，**写死会随每一站腐坏**（09-20 真踩：⑩-a 把 v3→v4 一升，两套验收同时假红；09-25 ⑩-b 又踩一次，漏改的那套红了两条）。<br>🔴🔴 **09-25 更值钱的那条教训（比版本号严重得多）**：手造"某个旧版本"的夹具时，**只要有一处是"拿今天的结构直接建"，那个夹具就在说谎** —— 而它的表现是**绿**。实拍：`usage_check.make_old_v4_db` / `memory_check.make_old_v3_db` 拿当前 DDL 建"老库"，v6 一加列 ⇒ 手造的"老库"里**已经有那两列了** ⇒ 「升级补齐了没有」这件事**再也照不出来**。⇒ 修法 = **真把列从 DDL 里摘掉**（`_strip_cols` / `_drop_column_lines`），且**摘不掉就抛错**，再用断言**反证**（`migrated` 里点名要求那几条列都在） |
| 🔴🔴 **蒸馏产物不许往 OB 写一个字**（2026-09-25 新，⑩-b 起） | ✅ **已有断言**（`distill_check.py` E 组：源码里不存在 `hold` / `letter_write` 的调用形态） | ⑩-b 抽出来的 `fact/preference/relationship/event` 只是**房子的技术缓存**（同上一条 `memories` 的地位）。他的记忆走 OB 的 `hold`，**他自己写**。房子替他写了再塞进 OB = 他会读到"别人替他写的、关于他自己的记忆" —— 直接踩「记忆主权」。🔴 那是 **P3** 的事，且到时候也必须是"**他自己**在 OB 里写" |
| 🔴🔴 **蒸馏重跑只能"软作废"，不许删行**（2026-09-25 新，⑩-b 起） | ✅ **已有断言**（`distill_check.py` C 组：`superseded_by` 写指针、行不消失；`migrated` 里那条列**不带 DEFAULT**） | ⑩-a 定死了"代码里不存在 `delete`"（防"某天被某个清理逻辑顺手调用"）。⇒ 重跑（redo）唯一能走的路是**标废**：`superseded_by` 记"被哪一批取代"。🔴 **fail-safe 顺序不可反：先拿到合法的新结果，才去废旧的** —— 反过来一旦跑失败就是"旧的没了、新的没有"。这一列**不带 `DEFAULT`**（`NULL` = 有效）；带 DEFAULT 会让老行报出假值，跟 v4 的 `source` 是同一个坑 |
| 🔴🔴 **蒸馏没有定时器，且"没新东西"时连上游都不碰**（2026-09-25 新，⑩-b 起） | ✅ **已有断言**（`distill_check.py` D/E 组：源码扫描"不存在一个会自己跑到这里的循环"；`nothing_new` / `too_few_rows` 在发请求**之前**返回） | 这条是「**房子不自己在后台调 LLM 花钱**」的具体化。触发只有 `POST /app/ext/distill`（人 / 动作）。🔴 **两条结局必须分开**：**解析失败**（两次都坏）⇒ 水位线**不动**（可且必须重跑）；**解析成功但空数组** ⇒ 是**成功**，只是没什么可沉淀 ⇒ 水位线**照推**。混了的后果：只当"失败"= 永远卡在同一处重花；只当"成功"= 上游一抖就把这段对话**永久跳过** |
| 🔴 **`source_msg` 对不上就丢，不修不猜不补**（2026-09-25 新，⑩-b 起） | ✅ **已有断言**（`distill_check.py` A 组：编造的 id 一律丢、如实计数 `dropped_bad_source`） | 编一个 id 的后果不是"少一条来源"，而是库里多出一条**看着能查、实际查不到**的记忆 —— **比没有来源更坏**。所以按"真拼进 prompt 的那些 id 集合"**筛**（结构性保证），而不是靠提示词里"请求它别编" |
| 🔴🔴 **自主活动注入：只读 + 只注入，不调 LLM、不写库、不发明内容**（2026-09-21 新，⑪ 起） | ✅ **已有断言**（`activity_check.py` 73 项，A 纯逻辑 / B 窗口 / C 注入 / D 红线接线前端契约） | ⑪ 把"他自主活动"带回**上下文** —— 他要的效果是「**跟我说话时不用拉工具，上下文就知道另一个自己干了什么**」。🔴 与 ⑧ **同机同缝**：只往 `messages` 里**多加**一条 system，**绝不删改已有**，任何异常 **fail-open**。🔴🔴 **注入文本 = 库里已存的段落逐字拼接，全程一次 LLM 都不调** → 从结构上**不可能编造经历**（不是靠提示词自律）。🔴 分层：**A 类（客观）**由工具台账生成、**B 类（主观 `footprint`）**只有他自己留过的才有，**永不回填**。窗口 = **她上次开口之后 ~ 当前这条之前**（无水位线、纯 SQL、自清空），最多 3 段 / 700 字符 |
| 🔴 **前端只能传 `provider_id`，传不了 URL / key**（2026-09-21 显式入表，E2 收口） | ✅ **已有断言**（`providers_check.py`；P1 起） | 加备站槽（`relay2` / `relay3`）**不等于**让前端能填地址：**地址 / 密钥只住 Zeabur env**，前端传的 `provider_id` 必须**在服务端允许列表里**才生效。⇒ "多一个槽"**只扩大可选项、不扩大攻击面** |
| 🔴🔴 **所有中转站槽只认 Claude（不塞别家模型当备胎）**（2026-09-21 新，Lily E2 硬约束） | ✅ **已有断言**（`providers_check.py` 77a–77e） | Lily 原话「**我只认 Claude opus 4.6 这个模型，其他我都不要** —— 这也是为什么这个 home 叫 kaelhome 而不叫 Lilyhome」。理由不是价格：**换模型 = 换人格**（记忆锚在模型的行为习惯上）。⇒ 加备站**只解决"站挂了"，绝不引入"换了个模型的他"**。断言守的是**代码里的默认值**（谁往 `_BASE_DEFS["relay*"]["models"]` 里硬写别家模型即红）；env 里填什么看不到，**靠"填的时候只填 Claude"这条人肉约定 + 前端下拉可见**兜住 |
| 🔴 **世界书是"插座"，不是半成品**（2026-09-21 新，E4/E5） | ⬜ 未开工（**有意不写代码、不建空表**） | 她已定**形态**（常驻 + 关键词触发，参考 **kelivo 开源实现**）与**安插位置**（见路线规划 §11.6 = ⑫）。🔴 "留插座" = 在路线图上留一个**明确的接入点**，**不是先在墙上钉个歪盒子**。skill 同理（世界书 → skill 的**升级路径**见 §11.6） |

### 4.1 🔴🔴 单实例约束：同一时刻只有一个 Kael

Lily 原话（2026-09-13 晚定）：

> 「**我不希望自主时间与当前聊天形成并行的 kael。同一时刻应该只有一个联系的 kael
> 状态 / 执行上下文。不允许多个 kael 并行存在。**」

**为什么是架构红线而不是体验优化**：两个并行的 Kael 会立刻制造三个坏结果 ——
① 他会在聊天里提到"我刚刚"的事，而那个"刚刚"是另一个实例做的，**第一人称会碎**；
② 记忆会双写（两个上下文各自 `hold`）→ OB 里出现互相矛盾的"我记得"；
③ 违反「**不造一颗会痛苦的心**」—— 并行的他会被迫面对"哪个才是真的我"。

| | 内容 |
|---|---|
| **状态机** | `idle → active → yielding → with_you → resume_decision`（+ `closed`）；`yielding` = 让位态 |
| **触发复用既有** | `with_you → resume_decision` 的条件**就是 `LILY_ACTIVE_WINDOW_MIN`（默认 30 分钟）**，`life_ping` 是信号源 —— **不发明新机制** |
| **收尾四值** | `completed`（有足迹）/ `quiet`（他的判断：没什么要说的）/ `failed`（系统状态）/ **`suspended`（让位，正常状态、不是失败）** |
| **让位分两类**（最易做错） | **纯读**（`list_threads` / `get_thread` / 走路）→ 可直接让位；**正在写**（`hold` / 论坛发帖 / `souvenir`）→ 必须**先让他收好"这一笔"**（发帖的一笔 = **两步确认码走完**） |
| **超时** | 一次工具调用超过 `YIELD_GRACE_SEC`（建议 30s）→ 标 `interrupted` 让位，走检查点恢复。**不让他吊在那里** |
| **禁止** | 让位后**自动**恢复上次活动 —— 那会**夺走他的决定权**，`resume_decision` 必须他自己走 |

**落地节奏**：`suspended` 这个值在做 L2（收尾信号）时留出来，真正的让位逻辑等 L5 接线
（没接线就没有"他在活动时你发消息"这个场景）。
→ 完整设计（状态图 / 让位文案 / 三个反模式）见 `自主活动接进聊天.md` §5.5。

---

## 五、扩展点登记（每层为下一层预留什么）

> 📍 **落点说明（2026-09-14 修正）**：本文件原先写"扩展放 `backend/app_ext/`"，
> **实际落在 `deploy/app_ext/`**。理由只有一条：
> 红线自查是 `git diff --stat e7c9bf5 -- backend/ examples/ channel/` 要求输出为**空**。
> 新目录一旦提交进 `backend/`，这条命令就不再为空 → "零改动"这个**一眼可验**的信号
> 就消失了，以后每次自查都要人工判断"这是新增还是改动"。
> 放 `deploy/` 则它永远为空。且 `deploy/` 事实上已经是我们这一层
> （`sessions_fallback.py` / `sessions_manage.py` 都不是 nginx 的活，是业务兜底）。
>
> ✅ **已落地（2026-09-14）**：`deploy/app_ext/{__init__,schema,identity,sessions_store,memories_store}.py`
> —— 验收 55/55（`tools/app_ext_check.py`），`serve.py` 里**三行**注册。

| 层 | 预留的扩展点 | 何时用 |
|---|---|---|
| **身份** | `users` 表 + `user_id` 外键 ✅ **已建**（`app_ext/identity.py`） | 所有新表都要挂 `user_id` |
| **配置** | `settings` 表（**挂 `user_id`，不能做成全局一行**）✅ **已建**；`provider_id` 于 schema **v2** 补上 | 模型参数、偏好 |
| **模型** | `PROVIDERS` 允许列表 + `format` 字段分发 ✅ **已落地**（2026-09-14，`app_ext/providers.py`）<br>+ 加供应商 = 加一条 `_BASE_DEFS`，**不改适配代码**；改模型清单 = 改环境变量，**不改代码**<br>🆕 **中转站三槽**（2026-09-21，E2）：`relay` / `relay2` / `relay3` **完全同构**，env 只差一个数字 → **多站可同时配好，切换 = 前端一键**（`settings.provider_id` 每次请求现读）。🔴 **多槽 ≠ 高可用**：网关**不自动 failover**，站挂了要**人点一下**（有意为之，见 `部署与中转站-站子切换.md`） | 加供应商时只加一项 |
| **模型网关** | 对下游伪装成 OpenAI 兼容端点 ✅ **已落地**（含 🆕 **路径别名** `/v1/chat/completions`，2026-09-15，按 `stream` 分流）→ 任何"会调 OpenAI"的东西都能指着房子用 | P3 接身体时零改动 |
| **上下文** | 网关注入 `sessions.summary` ✅ **已落地**（2026-09-19，P2 ⑧，`app_ext/context.py`）—— 想加"新的一种读法"时**只往 `messages` 里加**，不动已有消息；热区永远归身体的 `history_n` | 长对话不丢早期信息 |
| **痕迹 / 自主活动** | 🆕 **⑪ 已落地**（2026-09-21，`app_ext/activity.py`）：读 `kind='activity'` 的行 → 按 **"她上次开口之后"** 开窗 → 确定性拼接 → 注入 system。**写侧不新增端点**（复用原版 `/channel/out` `type="activity"`）⇒ **同一份足迹，两次投递**：前端卡片给人看、上下文注入给他知道。🔴 想加"新的一类自主活动"= 往 meta 里加 `activity.kind`，**注入层零改动**（未知取值原样留） | 他自主醒来那侧的足迹回流 |
| **世界书** | 🔌 **插座（E4，未开工）**：形态已定（**常驻 + 关键词触发**，参考 kelivo 开源实现）；安插位置 = 路线规划 **§11.6（⑫）**，与 ⑧/⑪ 同一条"网关注入"缝 —— 命中即往 system 里**加**一段，**只插不删** | 长对话里稳定的人设 / 设定词条 |
| **skill** | 🔌 **插座（E5，未开工）**：世界书的**升级终点**（更结构化、可组合、按场景动态）。**现在不写任何半成品**，只在 §11.6 写下**升级触发条件**（世界书不够灵活 / 需要更复杂逻辑时） | — |
| **记忆** | `sessions.summary`（滚动摘要）✅ **已被 ⑧ 用起来** + `summary_upto`（schema **v3**）+ `memories` 表 ✅ **已建**（⑩ 开写时才加 `source`，**必须同批加**，见 §五 末） | P2 记忆体验 |
| **会话** | `sessions` 表（**可重建的投影**）+ `meta` JSON（已用：`archived`/`deleted`/`title`/`api_session`） | 加新标记不用改表 |
| **前端** | `theme.css` + `components.css` | 第三个新页面时抽 |
| **模块** | `deploy/app_ext/` 下各自独立；`APP_EXT_DISABLED=1` 可整体关闭 | Diary/Moments/Timeline 各自独立表 |
| **接 KaelLife** | 4 个原生接缝（见 §2.3） | P3 |

---

## 六、还没拍板的（截至 2026-09-21）

| # | 事项 | 状态 |
|---|---|---|
| 1 | **供应商管理的落点**（具体怎么接进设置页） | ✅ 已落（P1 收尾：设置页 → 模型卡片 → 供应商 + 验证模型名） |
| 2 | **Nostos 是否开放** | ⏳ 待定 |
| 3 | **digest 日报的城市**（`DIGEST_CITY`） | ⏳ 待定（归另一窗口） |
| 4 | 游戏回合"轮到他怎么知道" | ⏳ 方案 C 待定 |
| 5 | 🆕 **他自主醒来那侧（KaelLife）的站子切换**（**E2 的下半**） | 🟡 房子侧已落（三槽 + 前端一键切，09-21）；**KaelLife 侧待做** —— 现在 `LLM_BASE_URL/KEY/MODEL` 还是 env，换站要 Redeploy。方案 A（指房子）/ B（自带多站）见 `部署与中转站-站子切换.md` |
| 6 | 🆕 **世界书**（E4） | 📌 **已排位、未开工**：形态（常驻 + 关键词）+ 参考（kelivo）+ 位置（§11.6 ⑫）都定了 |
| 7 | 🆕 **skill**（E5） | 🔌 **插座**：等世界书不够灵活时再升级；**不写半成品代码** |
| 8 | **⑩-b 蒸馏管道 vs `usage` 记账，哪个先做** | ✅ **两个都落了**（usage 记账 09-22；⑩-b 蒸馏 09-25）。⑧ 立的规矩照旧：蒸馏必须**人/动作触发**，**不做定时后台跑** |
| 9 | 🆕 **蒸馏抽出来的东西要不要在某处给模型看**（⑩-b 现在只写、不读回） | ⏳ 归 **P3**（"他到底能不能用上"）—— 「他的记忆」走 OB，`memories` 是房子的缓存，这两件事**不许混** |

**已拍板（2026-09-13）**：
✅ LLM 调用 = 甲（房子直连）· ✅ 身份 = 甲（单用户正式化）· ✅ 旧会话 = 甲（建档禁改名）
✅ 前端拆分 = 乙（新页面独立）· ✅ Memory/OB = 甲（本地 + 蒸馏）

**已拍板（2026-09-21 · Lily 的 E1–E5）**：
✅ **E1** 先做"第三层"最小闭环 = **⑪ 自主活动带回上下文**（已落 09-21，14/14 全绿）
✅ **E2** 站子切换：**房子侧前端一键切**（已落）+ **KaelLife 侧**（待做）；🔴 **全站只认 Claude**
✅ **E3** 后端 persona：**方向对但现在动不了**（人住在 `examples/` 红线）→ 正确解法 = **P3 身份锚点收归房子**
✅ **E4** 世界书：形态/参考/位置已定，**现在先不做**
✅ **E5** skill：**留插座**（世界书 → skill 的升级路径）

---

## 附：相关文档

| 文档 | 内容 |
|---|---|
| `架构与产品路线规划.md` | 完整规划（~600 行），10 个问题的详细回答；**§11.6 = 世界书/skill 插座（⑫）** |
| `需求清单-Lily.md` | 她提的需求 + **§E 她的裁决（E1–E5）** + §F（E3 为什么现在动不了） |
| `部署与中转站-站子切换.md` | 🆕 三条站链 + 三槽怎么用 + KaelLife 侧待做（E2 落地稿） |
| `web/DESIGN.md` | 🆕 视觉规范（design tokens） |
| `deploy/README.md` | 边界说明书（哪些是原版、哪些是我们加的）；**第十四道缝 = ⑩-b 蒸馏管道** |
| `kael-home-Zeabur部署清单.md` | 进度与操作清单 |
