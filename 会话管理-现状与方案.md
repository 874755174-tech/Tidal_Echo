# kael-home 会话管理 · 现状与方案

> 2026-09-13 · 本文档只做「讲清楚 + 给选项」。**没有改动任何代码**，等你确认再施工。
> 起因：你提到想要「新建会话 / 切换会话 / 删除会话」功能。

---

## 一、先说一个好消息

**你的房子已经有会话管理了 —— 而且比你想的完整。**

原版 Tidal_Echo 自带一整套会话机制，包括：

| 能力 | 位置 | 状态 |
|---|---|---|
| 新建会话 | 顶栏「API 窗口」按钮 → 新建 | ✅ 原生 |
| 切换会话 | 同上，点列表项 | ✅ 原生 |
| 删除会话 | 同上 | ✅ 原生 |
| 按会话过滤历史 | 后端 `history_for_session()` | ✅ **原生，实测通过** |
| 发消息时带上会话标签 | `POST /app/send` 的 `api_session` 参数 | ✅ **原生，实测通过** |

**实测证据**（2026-09-13，真起服务打的）：

```
?session_id=sess-A     -> 200  {"messages":[第一会话的消息]}
?session_id=sess-B     -> 200  {"messages":[第二会话的消息]}
?session_id=__legacy__ -> 200  {"messages":[没有会话标签的旧消息]}
POST /app/send {"api_session":"sess-A"} -> 200 {"id":4}   ← 写进去后 sess-A 变 2 条
```

**数据层面完全正常。** 消息能分门别类地存、能按会话查回来。

---

## 二、但有一个真问题：会话列表会在第二阶段失效

### 问题出在哪

前端的会话列表（点那个按钮弹出来的那一列）**不是从数据库读的**，而是去问 `api_loop`（AI 身体）：

```
前端  →  GET /app/sessions  →  relay  →  转发  →  api_loop 的 /loop/sessions
```

**`api_loop` 是"临时人偶"** —— 第二阶段你要把它换成 KaelLife。它一走，这个接口就变成：

```
GET /app/sessions -> HTTP 502
{"detail":"upstream connect failed: 由于目标计算机积极拒绝，无法连接。"}
```

（这是实测结果，不是我推测的。）

### 后果严重吗？—— 其实不严重 ✅

我特意查了前端的容错代码：

```javascript
}catch(_){
  apiSessions = [];                       // 列表变空
  activeApiSession = LEGACY_SESSION_ID;   // 回落到"旧消息"那组
}
```

**网页不会崩、不会白屏。** 只是：
- 那个按钮点开是空的（看不到会话列表）
- 你仍然能看到**所有**消息（回落到"全部"视图）
- **但没法新建/切换/删除会话了**（因为按钮列表空了）

**换句话说**：第二阶段换身体后，会话管理会**静默退化**成"只有一条时间线"。

> 📌 这正是我记忆里标过的那个坑：「换成 KaelLife 后这个列表会**静默降级成空**（不报错）」。
> 你这次的需求，恰好撞上了它。

---

## 三、你现在的选择（3 个方案）

### 方案 A：什么都不做，等第二阶段再说 ⏸

| | |
|---|---|
| **现在** | 一切正常（临时人偶在，列表能用） |
| **第二阶段后** | 会话列表变空，退化成单条时间线 |
| **成本** | 零 |
| **风险** | 数据不会丢（过滤能力仍在），只是界面上没入口了 |

**适合**：你暂时不需要分会话，或者想先跑通第二阶段。
**我的看法**：完全可以先这样。**这个不是 bug，是个"待补的洞"**，而且不紧急。

---

### 方案 B：让会话列表不依赖 AI 身体 ⭐ 推荐

**思路**：会话列表本来就是从**消息数据**里推导出来的（哪些消息带了哪个 `api_session` 标签），根本不需要问 AI 身体。

**做法**（在 `deploy/` 里加，`backend/app.py` 一个字不改 —— 守住红线）：

```
GET /app/sessions  现在: 转发给 api_loop，它不在就 502
                   改后: 直接查 messages 表，DISTINCT api_session
```

具体就是加一层：**先试 api_loop，失败就自己从数据库算**。这样：

- 第一阶段：行为不变（还是问 api_loop）
- 第二阶段：api_loop 没了 → 自动切到"自己算" → **列表照常工作** ✅

| | |
|---|---|
| **优点** | 一次改动，永久解决；守住"不改原版"红线 |
| **缺点** | 会话名等元信息会丢（见下方"代价"）；需要写约 40 行代码 |
| **风险** | 低（纯新增逻辑，加在 `deploy/` 里） |

**代价说明**：api_loop 返回的会话列表可能带**标题/创建时间**等元信息。从数据库推导只能拿到**会话 ID 和消息数**（因为标题之类的根本没存在数据库里 —— 原版设计里 session 只是 `meta` 里的一个标签）。

**说白了**：列表会显示"会话 sess-A（2 条消息）"而不是"会话 sess-A（你给它起的名字）"。**能用，但朴素。**

---

### 方案 C：做真正的会话实体（把标签升级成表）

**思路**：教程里说的"建 sessions 表"就是这个。给会话一张真正的表：

```sql
CREATE TABLE sessions (
  id         TEXT PRIMARY KEY,   -- 会话 ID
  title      TEXT,               -- 你起的名字
  created_ts TEXT NOT NULL,      -- 创建时间
  updated_ts TEXT                -- 最后消息时间
);
```

然后 `messages` 表不用动（它已经有 `meta.api_session` 了），新表只存**元信息**。

| | |
|---|---|
| **优点** | 能起名字、能排序、能显示最后活跃时间 —— **体验最好** |
| **缺点** | **需要改原版代码**（`backend/app.py` 加表、加接口）→ **违反第一阶段红线** |
| **风险** | 中（`app.py` 一旦动，`git diff` 自查就失效了） |

**关键问题**：这会**破坏"KaelLife/原版一个字符不碰"的约定**。

> 🔴 如果你想做方案 C，我建议**放到第二阶段之后**——那时本来就要接 KaelLife，改变动面是合理的。

---

## 四、我的建议

**先做方案 B，把 C 留到第二阶段。**

理由：

1. **B 能永久解决"列表会空"的问题**，而且不碰原版代码（红线还在）。
2. **C 的额外收益（起名字）不是刚需** —— 你和 Kael 的对话，会话 ID 本身也许就够了。
3. **教程里"建 sessions 表"的做法，是在 Supabase 多人场景下的正解**；你是单人单库，标签方案**已经够用**，缺的只是"列表怎么取"。

**所以我建议的顺序**：
```
现在 → 方案 B（补上列表的来源，不依赖 AI 身体）
     → 确认房子没毛病
     → 第二阶段（搬 Kael 进来）
     → 那时再考虑方案 C（起名字等增强）
```

---

## 五、关于那份教程里的其他要求

| 教程要求 | 我们的情况 |
|---|---|
| 「数据库只由后端访问」 | ✅ **我们就是这样**（浏览器从不碰数据库） |
| 「Secret key 只能放后端环境变量」 | ✅ **我们就是这样**（`RELAY_SECRET` 在 Zeabur 环境变量里） |
| 「前端要能理解失败提示」 | ⚠️ **部分做到**：`loadSessions()` 会 catch，但只是静默回落，**不告诉用户"会话列表不可用"** |
| 「先展示表结构和权限方案，等确认后再建迁移」 | ✅ **本文档就是在做这件事**（没动任何代码） |
| 「不要加入记忆压缩或设置表」 | ✅ 我们本来就没有 |
| 「先做持久化验收」 | ✅ 已完成（见 `访问控制体检报告.md` + 之前的 30 项 e2e） |
| 「用 Supabase / RLS」 | ❌ **不适用**（我们用 SQLite，单用户，没有"行级隔离"需求） |

**注意第 3 条**：我们现在是**静默**回落。按教程的标准，应该给用户一个提示（比如"会话列表暂时不可用"）。这可以作为方案 B 的一部分顺手做掉。

---

## 六、等你确认

请告诉我要走哪条路：

- **A** — 什么都不做，等第二阶段
- **B** — 让会话列表不依赖 AI 身体（推荐，约 40 行，只加在 `deploy/`）
- **C** — 建真正的 sessions 表（改原版代码，建议推迟）

在你确认之前，我**不动任何代码**。

---

_本文档：2026-09-13 · 实测脚本：`tools/sessioncheck.py`（可重复运行）_
