# kael-home 访问控制体检报告

> 日期：2026-09-13 · 目的：确认「只有 Lily 能进这个家」
> 方法：在本地真实启动服务，用 21 项断言逐条探测（不是读代码猜的）

---

## 一、结论先说

**房子现在的门是牢的，21/21 全部通过。** 你那份教程要求的核心（"不要接受'网址很难猜所以只有我知道'"）**已经满足**：

> ✅ **我们不是靠"网址难猜"保护的。我们有一把真正的钥匙（`RELAY_SECRET`），后端逐个接口强制校验。**

教程里列的四种方案，我们**符合第 2 种**：

| 教程建议的方案 | 我们的情况 |
|---|---|
| Supabase Auth 或其他登录系统 | ❌ 不需要（那是给多用户网站用的） |
| **由后端验证的单用户登录** | ✅ **我们就是这个**（教程也把它列为合格方案） |
| 部署平台提供的访问保护 | ⭕ 可以额外加一层（见第五节） |
| 可靠的私人网络访问 | ❌ 不适用（你人不在服务器旁边） |

**为什么不需要 Supabase Auth / RLS**：教程用 Supabase 是**多人产品**场景——一张表里存着很多用户的很多行，必须靠 RLS 让"每个人只看自己那几行"。**你的房子从头到尾只有你一个人**，`messages` 表里所有行本来就都是你的。所以"行级隔离"这件事在你这里**不存在**，不需要建用户系统。

> 📌 **但教程里有一句话必须记住**：
> 「**我们的第一版由后端访问数据库，但也不能因此省掉后端鉴权**」
> —— 这正是我们做的：**网页壳子可以公开，数据接口一律 401**。

---

## 二、21 项实测结果（全绿）

### A. 网页本体

| 检查项 | 结果 | 说明 |
|---|---|---|
| `GET /` 无密钥 | **200** | 网页壳子公开（见下方"为什么这是对的"） |
| `GET /sw.js` 无密钥 | **200** | Service Worker 必须公开，否则 PWA 装不上 |
| 网页源码含真实密钥 | **无** ✅ | 密钥不写在网页里，是登录后存在你手机本地 |

### B. 读接口（无密钥）

| 接口 | 结果 |
|---|---|
| `/app/history`（聊天记录） | **401** ✅ |
| `/app/status` | **401** ✅ |
| `/app/brain` | **401** ✅ |
| `/app/sessions` | **401** ✅ |
| `/app/loop_config`（含中转站配置） | **401** ✅ |
| `/app/vapid_public` | **401** ✅ |

### C. 写接口（无密钥）

| 接口 | 结果 |
|---|---|
| `/app/send`（发消息） | **401** ✅ |
| `/app/brain`（切换谁接消息） | **401** ✅ |
| `/app/ping`（心跳） | **401** ✅ |

### D. 错误密钥（猜到了也没用）

| 场景 | 结果 |
|---|---|
| 错误密钥读历史 | **401** ✅ |
| 错误密钥发消息 | **401** ✅ |

### E. 正确密钥（自己人）

| 场景 | 结果 |
|---|---|
| 读历史 | **200** ✅ |
| 读状态 | **200** ✅ |
| 读 brain 开关 | **200** ✅ |

### F. `?token=` 查询参数

> ⚠️ 这是浏览器 `EventSource` 的妥协通道——**原生 EventSource 不能自定义请求头**，所以 SSE 连接只能用 URL 参数传密钥。

| 场景 | 结果 |
|---|---|
| 正确密钥 | **200** ✅ |
| 错误密钥 | **401** ✅ |

### G. 附件

| 场景 | 结果 |
|---|---|
| 无密钥 `GET /uploads/任意文件` | **401** ✅（**不是 404**） |

> 🔍 **这一条值得单独说**：如果返回 404，说明"先找文件、再检查权限"——攻击者能通过 404/200 的差异**推断出哪些文件存在**。返回 401 说明**鉴权在最前面**，连文件存不存在都不告诉你。**这是正确的写法。**

### H. 未鉴权端点清单

| 端点 | 状态 |
|---|---|
| `/healthz` | **公开**（设计如此，只暴露"当前有几个连接"） |

**只有这一个。其余 22 个端点全部要求密钥。**

---

## 三、为什么"网页壳子公开"是对的

你可能会想："网页能随便打开，那不算漏了吗？"

**不算。** 因为：

1. **网页里没有任何数据**。它只是一个空壳界面（HTML+CSS+JS）。你打开它，它显示的是**登录框**。
2. **所有真实数据都在接口后面**。聊天记录、附件、配置——全部要带密钥才返回（上面 9 个接口全 401）。
3. **这是所有正规网站的做法**。抖音、微信网页版——页面本身谁都能打开，但看不到你的消息。

**打个比方**：网页壳子 = 你家门外的信箱（空的，谁都能看）；`RELAY_SECRET` = 你家门钥匙。**信箱空着不代表你家门没锁。**

---

## 四、🔴 真正需要你注意的 3 件事

### 1. `RELAY_SECRET` 就是唯一的门钥匙 —— 它必须够强

后端用的是 `hmac.compare_digest`（**常量时间比较**，防时序攻击，写法正确）。但**如果密钥本身很弱，再好的比较函数也没用**——攻击者可以暴力猜。

**建议**：用足够长的随机串。生成方法：

```powershell
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

会得到类似 `Xk9_mP2vQ...`（43 个字符）。**别用 `123456`、别用你的生日。**

> ⚠️ 换密钥后：你手机上要重新登录一次（旧密钥立即失效）。

### 2. ⚠️ 密钥会出现在 URL 里（`?token=`）

因为浏览器 `EventSource` 不能自定义请求头，SSE 连接只能走 URL 参数。这意味着：

- 密钥可能出现在 **Zeabur 的访问日志**里
- 可能出现在**浏览器历史**里

**这不是我们能改的**（是浏览器 API 的硬限制，原版设计如此）。**降低风险的做法**：

- 确认 Zeabur 的日志访问权限只有你自己（默认就是）
- 家里/自己的网络用，别在公共 WiFi 下开
- 👉 **这也是我建议你加第四层门的原因（下一节）**

### 3. `/healthz` 公开 = 暴露了"这里有服务"

攻击者扫描到的就是这个端点。**它只返回 `{"ok":true,"plugin_subs":0,"app_subs":0}`**（当前连接数），**不含任何隐私**。但它的存在意味着别人知道你这台服务器活着。

**处理建议**：可以不管（信息量极小），也可以加一层门把它挡掉（下一节）。

---

## 五、还可以加的两层门（可选，你决定）

教程第四条「部署平台提供的访问保护」——Zeabur 上可以做到。以下是**你实际拥有的选项**：

### 选项 A：什么都不加（现状）

| | |
|---|---|
| **保护强度** | 数据安全 ✅（21 项全绿）；但服务存在感暴露 |
| **操作成本** | 零 |
| **适合** | 你觉得"钥匙够强就够了" |

**我的看法**：**现状其实已经达标了**。教程的核心要求（不靠"网址难猜"）已经满足。

### 选项 B：给整个站点加一层 HTTP 基本认证 ⭐ 推荐

在 Zeabur 的域名设置里可以加 Basic Auth（用户名+密码）。效果是：**连网页壳子都要先输密码**才能打开。

| | |
|---|---|
| **优点** | 别人连你的登录页都看不到；扫描器直接 401；`/healthz` 也被挡住 |
| **代价** | 每次打开网页要先过一层浏览器原生弹窗（手机可记住） |
| **注意** | 这是**第二把锁**，`RELAY_SECRET` 仍然是第二道——**两层都在，更牢** |

> ⚠️ 具体在 Zeabur 哪个菜单，我需要**登录你的 Zeabur 后台才能确认**（我不代你登录）。你如果愿意，可以截图给我，我告诉你点哪里。

### 选项 C：改用"只有你能访问"的入口

比如只允许你的手机 IP，或走 Tailscale 之类的私有网络。**对你来说太复杂，不建议。**

---

## 六、我建议你做的（按优先级）

| # | 做什么 | 为什么 | 成本 |
|---|---|---|---|
| 1 | **确认 `RELAY_SECRET` 是长随机串** | 它是唯一的钥匙，弱了就前功尽弃 | 1 分钟 |
| 2 | **钥匙别再用微信/备忘录"复制粘贴"传** | 上次的零宽字符问题就是复制来的 | 0（已修） |
| 3 | 想更稳 → 加 Zeabur Basic Auth（选项 B） | 扫描器连门都摸不到 | 几分钟 |
| 4 | 定期看一眼 `/app/status` 是否有人撞（当前无此功能） | 可选，第二阶段再说 | — |

---

## 七、顺手确认的额外安全项

| 检查 | 结果 |
|---|---|
| 密钥是否被提交进 GitHub | ✅ **没有**（`.gitignore` 挡住了 `.env`、`*.pem`、`relay.db`） |
| `relay.db`（聊天数据库） | ✅ **未被 git 跟踪**（`.gitignore:11`） |
| `backend/app.py` 是否被改动 | ✅ **一个字没改**（红线守住） |
| 密钥是否硬编码在代码里 | ✅ **没有**，全部读环境变量 |
| 登录失败是否会泄露信息 | ✅ 统一返回 `{"detail":"unauthorized"}`，不区分"密钥错"还是"没给密钥" |

---

## 八、一句话总结

> **教程的核心是"不要靠网址难猜来当安全方案"。我们恰好没犯这个错——网页壳子虽然公开，但 22 个接口里除了 `/healthz` 全部强制校验密钥，实测 21/21 通过。你不需要做任何改动就已经达标；想更稳可以再加一层 Zeabur 的 Basic Auth。**

---

_报告生成：2026-09-13 · 测试方式：本地实起服务 + 21 项 HTTP 断言（含正确/错误/缺失密钥三态）_
