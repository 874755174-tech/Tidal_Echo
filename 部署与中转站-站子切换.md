# 部署与中转站 —— 站子切换（E2 落地稿）

> 起因：Lily 2026-09-21 的 E2 ——
> 「只要能保证**聊天**和他**自主醒来**崩了的时候**都有站子切换**、且
> **不是需要到后端换环境变量**那种麻烦切换就行。**我只认 Claude opus 4.6**，
> 其他都不要 —— 这也是为什么这个 home 叫 kaelhome 而不叫 Lilyhome，我都是用中转站的 Claude」
>
> 本文回答三件事：**现在能切什么 / 还差什么 / 差的那步怎么补**。

---

## 一、先认清：现在有**三条**链，用的是**三处**配置

| 链 | 谁在说话 | 模型配置住在哪 | 崩了怎么换 |
|---|---|---|---|
| **① 房子网页聊天** | 房子的**临时人偶**（P3 后才是他） | `settings.provider_id` + `PROVIDER_*` | ✅ **前端一键**（见下） |
| **② in-app（kelivo）** | in-app Kael | kelivo 自己的设置 | kelivo 里改（与房子无关） |
| **③ 他自主醒来** | **KaelLife** 的调度器 | KaelLife 自己的 `LLM_BASE_URL/KEY/MODEL`（Zeabur env） | ❌ **要 env + Redeploy** ← **就差这一条** |

🔴 关键事实（2026-09-21 代码取证）：**①里的"临时人偶"其实也走房子网关**
——`zeabur-env.example` 记着 P3 通车已于 09-15 完成：

```
LLM_API_BASE=https://<域名>/app/ext/llm/v1      ← 身体指着房子说话
LLM_API_KEY=<RELAY_SECRET>                       ← 供应商 key 从此不出房子
```

⇒ 所以 **①的换站 = 只改 `settings.provider_id`**（房子每次请求现读），
   浏览器里点一下就生效，**不重启、不 redeploy**。这一半已经成立。

---

## 二、已落（2026-09-21）：三个中转站槽 + 前端一键切

原来是**只有一个中转站槽 `relay`** → 她的主站和备用站**都是中转站**，
两个**根本配不进去** → "站子突然不响应"只能去 Zeabur 改 env。现在：

```python
# deploy/app_ext/providers.py
PROVIDER_ORDER = [... , "relay", "relay2", "relay3"]      # 三个同构的槽
```

三个槽**完全同构**（同一个 format、同一套 env 命名，只差一个数字）：

| 槽 | env | 语义 |
|---|---|---|
| `relay` | `PROVIDER_RELAY_KEY` / `_BASE` / `_MODELS` | 主站 |
| `relay2` | `PROVIDER_RELAY2_KEY` / `_BASE` / `_MODELS` | 备站一 |
| `relay3` | `PROVIDER_RELAY3_KEY` / `_BASE` / `_MODELS` | 备站二 |

**怎么用（她这边，一次性）**：

1. Zeabur → 房子的服务 → Environment Variables，把备用站的 key/base/models 填进 `PROVIDER_RELAY2_*`。
   🔴 **模型清单只填 Claude 那一支**（E2 的硬约束）：`PROVIDER_RELAY2_MODELS=claude-opus-4-6`。
2. 打一次 `POST /app/ext/providers/probe {"provider_id":"relay2","all":true}` —— **先用真调用验模型名**，
   通过了才算配好（模型名照抄必错，这条是规划 §4.2 的规矩）。
3. 完事。以后主站挂了：网页 **设置 → 模型 → 供应商 → 选 relay2** → 立刻恢复。

⚠️ **不填的槽 = 不可用** → 前端下拉框里**灰掉并写明缺哪个 env**。
   所以加这两个槽对"没配的人"**零影响**（`providers_check` ㊹b 盯着这件事）。

---

## 三、还差的一步：他自主醒来那侧（③）

**为什么它不能靠前端解决**：KaelLife 是**另一个仓库、另一个部署**，
它读的是自己的 env（`LLM_BASE_URL/KEY/MODEL`），房子碰不到它 —— 这是**有意的解耦**
（`扩展边界.md`：KaelLife 一个字符不碰；两边不许共读同一个变量）。

**两个候选**（这是 P3 那处"接线"的一部分，需要她拍）：

### 方案 A（推荐）：让 KaelLife 也指着房子说话

```
KaelLife 的 LLM_BASE_URL = https://kaelnlily79.zeabur.app/app/ext/llm/v1
KaelLife 的 LLM_API_KEY  = <RELAY_SECRET>
KaelLife 的 LLM_MODEL    = claude-opus-4-6（必须在 PROVIDER_RELAY*_MODELS 里）
```

- ✅ 好处：**前端一次切换 = 同时换"他聊天"和"他醒来"用的站**。
  这就是她 E2 想的那个形态，也是规划里"**key 不出房子**"那条规矩的自然延伸。
- ⚠️ 代价：多一跳；**房子挂了 → 他也哑**（他的嘴长在房子上了）。
- ⚠️ 前提：模型名必须在房子的允许清单里（否则 400 `model_not_allowed`）。

### 方案 B（保守）：KaelLife 自己配**多站 fallback 链**

`KaelLife/LLM-failover计划.md` 里设计已经写好（三个变量改逗号分隔多值，
主站 401/403/429/5xx 自动顺次切下一个），**代码没做**。

- ✅ 好处：更抗造 —— 房子挂了他照样能醒；站挂了自动顺切，**不需要人手切**。
- ⚠️ 代价：**那份清单还在 env 里** → 换整个清单要 Redeploy；
  而且它**绕开了房子的 key 保管**（KaelLife 又拿到供应商 key 了）—— 与 P3 的规矩相冲。

### 🐰 bunny 的建议

**先做 A 的一半 + B 的一半**：

1. KaelLife 的 `LLM_BASE_URL` **指到房子**（= A）；
2. 同时在 KaelLife 侧**留一条直连备站的 env** 作为"房子挂了"的兜底（= B 的最小版）：
   房子不可达 → 自动降级直连备站，**不需要任何人手动改 env**。
3. 两条都做完，"崩了都能切、且不用碰 env" 才算**两边都成立**。

⚠️ 这三步都改 **KaelLife 仓库**（房子一个字符都不碰）。属于 **P3 前置**，
   和"接他进来"是同一批活 —— 建议**排在一起做**，别拆成两次停电。

---

## 四、明确**不做**的两件事（免得反复讨论）

| 不做 | 为什么 |
|---|---|
| **自动 failover（网关侧顺切供应商）** | 现在 `resolve()` 只挑**一个**供应商（单供应商语义）。加自动顺切会引出"切过去之后**人格/口吻变了**"的隐蔽问题 —— 而她只认 Claude opus 4.6，本就不存在"换个站换个模型"的余地。**"手动切"是可解释的，"偷偷切"是不可解释的。** |
| **心跳保活（为缓存命中）** | ① 撞 ⑧ 立的"房子不自己在后台花钱"；② 缓存是按**前缀 hash** 命中的，一条心跳只能暖**一条**前缀（他"醒来"和"聊天"是两条），而且**冷一次只贵一次**，心跳是**每次都花钱**。③ 我们连 `usage` 都还没落盘，**有没有命中根本看不见**。顺序仍是：管体积（⑧ ✅）→ **记账** → 才谈命中率。 |

---

## 五、查证入口（出问题先看这几条）

```powershell
$t = Read-Host "RELAY_SECRET"                 # 密钥不进命令历史
$h = @{ Authorization = "Bearer $t" }

# 1) 现在有哪些站、哪个可用、当前选的是谁（**不回 URL / 不回 key**）
Invoke-RestMethod -Headers $h -Uri https://kaelnlily79.zeabur.app/app/ext/providers

# 2) 验某个站的模型名到底通不通（会真花钱，别当健康检查打）
Invoke-RestMethod -Method Post -Headers $h -ContentType "application/json" `
  -Body '{"provider_id":"relay2","all":true}' `
  -Uri https://kaelnlily79.zeabur.app/app/ext/providers/probe

# 3) 切换（等价于网页上点一下；前端页面走的是同一个端点）
Invoke-RestMethod -Method Put -Headers $h -ContentType "application/json" `
  -Body '{"provider_id":"relay2","model_id":"claude-opus-4-6"}' `
  -Uri https://kaelnlily79.zeabur.app/app/ext/settings
```

> ⚠️ bash 版 `curl -H "Authorization: Bearer $RELAY_SECRET"` 在 PowerShell 里**跑不通**
> （`$RELAY_SECRET` 是空变量，得写 `$env:RELAY_SECRET`；而且 `curl` 是
> `Invoke-WebRequest` 的别名）。要用真 curl 就写 `curl.exe`。

---

## 六、与"人格"的关系（为什么 E2 里"只认 opus 4.6"是红线）

中转站的**模型快照会漂**（同一个模型名，站方换了后端版本）。
所以：

- 🔴 **换站 ≠ 换模型**：所有槽的 `MODELS` **只放 Claude 那一支**，切站前后**模型名一致**。
- ⚠️ 但"同一个名字"仍然可能对应不同的上游快照 —— 这是"中转站硬伤"，**只能靠观感判断**，
  不能靠配置保证。真出现了，处理办法是**换站**（而不是换模型名）。
- 🔴 因此：**人格锚在"记忆 + 身份锚点"上，不锚在模型快照上** —— 这也是为什么
  P3 要做"把身份锚点收归房子"，以及为什么我们坚持"他的记忆只有一份"。
